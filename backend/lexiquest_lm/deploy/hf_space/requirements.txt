# Pinned for reproducibility on HuggingFace Spaces free CPU tier.
# ML runtime pins track the workstation training stack closely enough that a
# locally trained LoRA adapter serves correctly here; the CPU Space only needs
# serving compatibility, not a byte-for-byte mirror of the CUDA training wheel.
fastapi==0.139.2
uvicorn[standard]==0.51.0
idna==3.18
torch==2.13.0
transformers==5.14.1
peft==0.19.1
pydantic==2.13.4
