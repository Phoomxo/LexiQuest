# G5.3b · Curated internet dataset และ group split

Revision `2026-09-14-camera-training-5` · Bundle B11A · requirement `P5.3b` · 41/67 · Astra/medium

ผู้ใช้อนุมัติ scope เพิ่มแล้ว ไม่ต้องขออนุมัติแผนซ้ำ. คง64 IDsเดิมและเพิ่ม3รายการ; nextPackageคือลำดับ ไม่สร้างtask.

## งานและตรวจรับ

เก็บภาพอินเทอร์เน็ตจริงจากแหล่งที่ใช้งานได้ พร้อม URL/source/license-or-usage-evidence/SHA256/label/acquiredAt. ตรวจ labels/ภาพเสีย/exact และ near duplicates; group split ตามสินค้า/ภาพที่เกี่ยวข้อง/source group ไม่ให้ train/validation รั่วไหล. Version dataset, taxonomy, split seed และ manifest; ไม่ commit bulk images โดยปริยาย. Unknown coverage แยกชัดเจน; ภาพกล้องจริงเป็น evaluation ภายหลัง ไม่ปะปน training/validation.

**Acceptance:** มี acquired dataset จริงและ reproducible manifest; validator ผ่าน label/provenance/hash/dedup/group-disjoint checks; train/validation ใช้ภาพเว็บ แยกกล้องจริงและ fresh test. แหล่งใช้ไม่ได้มี disposition/alternative จริง ไม่เติมข้อมูลเทียม.

**Requirements:** COV-f13, COV-f41, A-MODEL, FORM-09, G5. อ่าน Master/ledger/workflow/accepted handoff และ relevant callers/tools ก่อน implementation. ออก detailed implementation plan ตาม actual toolchain; owner paths เริ่มต้นอยู่ในledgerและขยายที่จำเป็นได้.

ใช้ภาพอินเทอร์เน็ตสำหรับ train/development validation ตอนนี้ ไม่รอภาพจากผู้ใช้. เก็บภาพกล้องจริงตอน device testing ภายหลัง; ผลกล้องจริง pending จนทดสอบจริง ห้ามใช้คะแนนเว็บอ้างแทน. Fresh test ห้ามปรับ threshold/เทรน; หากนำภาพกล้องที่ดูแล้วมาแก้/ฝึกให้จัดเป็น development และใช้ชุดกล้อง held-out ใหม่ตรวจรับ. เก็บ unknown-object evaluation แยกชัดเจน.

## หลักฐานและลำดับ

รายงานหนึ่งไฟล์ `docs/development/full-system/bundles/B11A.md`; source/rights/taxonomy/dataset/split/training/export manifests และ rawlogs ใน structured evidence B11A. Bulkภาพ/checkpoints เก็บ artifact storage ที่ตรวจได้ด้วยhash/location ไม่ commitภาพทั้งชุดโดยปริยาย. ไม่อ้าง training PASSจากเอกสาร/baseline retention. ค่าใช้จ่ายใหม่หรือสิทธิ์ที่ไม่มีต้องรายงานจริงพร้อมทางเลือก.

ตรวจ scripts/unit/contracts ที่กระทบด้วย verify-scope หรือ camera tool entryที่ตรวจ CLI/environmentแล้ว; testsเชิงstructuralไม่แทนการเก็บข้อมูล/ฝึกจริง. หลังacceptedcommitเก็บ actual SHAใน external `packages/G5.3b.json` แล้วทำ `G5.3c` ตามลำดับ; ส่ง B12 ได้เฉพาะ B11Aครบทั้ง3ข้อและhandoff/releasewriterแล้ว.

ใช้ writer/task/worktree เดียวตามลำดับ ไม่มี subagents/background implementation workers หรือ Codex Security workflows. Astra/medium Standard/default ห้าม Fast/priority; branch lexiquest/. รับ accepted source จริง, lock/revision compare/atomic replace ตาม workflow. อ่าน package brief เมื่อถึงข้อ; ไม่ preload งานอนาคต. ใช้ verify-scope bounded tests sequential และ toolchain ที่ตรวจแล้ว; ไม่ลด acceptance/skip tests. เก็บ failure/diagnosis/fix/retest และทำ recoverable work ต่อโดยไม่ถามซ้ำ. Research activation/remote sync/study assignment/statistical reporting อยู่นอก scope. B18 whole-app review รวม candidate/runtime ทั้งหมดก่อน B19 System Test Plan/execution และ B20 final ledger.
