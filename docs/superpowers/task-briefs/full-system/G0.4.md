# G0.4 · ยืนยันตัวตรวจแบบมีขอบเขตและการ reuse ผล

Revision `2026-09-13-bundles-4` · requirement `P0.4` · 4/64 · `gpt-6-astra` / `medium`

**Accepted history — ไม่ย้อนทำหรือdispatch**

## รับ requirement เมื่อถึงลำดับ

`G0.4` acceptedก่อนเปลี่ยนworkflow; SHA/receiptอยู่index.acceptedHistory. Technical contentด้านล่างเก็บเป็นrequirement/evidence reference. ไม่รับseed7712หรือdispatchซ้ำ หากdefectใหม่กระทบให้currentbundleทำremediationพร้อมownerreference

อ่าน [AGENTS](../../../../AGENTS.md), [Active Index](../../../development/full-system-active-index.md), [Rule Register](../../../development/2026-09-13-rule-supersession-register.md), [Master](../../plans/2026-09-13-lexiquest-full-system-master-plan.md) และ [ledger](../../../development/2026-09-13-full-system-work-ledger.json) เฉพาะส่วนที่เกี่ยวข้อง. Technical scope/acceptance/testsด้านล่างคงจากacceptedG0.5โดยไม่ลดเกณฑ์

## ขอบเขตและผลที่ต้องได้

ทำ command registry ของ Targeted/Subsystem/Release, ตรวจ parameter และ input closure ของ F2; บันทึก source/dependency/config/test selection พร้อม log directory; เลือก gate ที่ยังไม่เคยผ่าน source เดียวกัน

**Requirements:** PLAN-02, PLAN-11, G0

**Owner paths เริ่มต้น:** `tool/cli/verify-scope.ps1`, `tool/final_test_plan/generate_final_test_plan.dart`. ขยายไฟล์ที่จำเป็นได้พร้อมเหตุผล/consumer/compatibility; ไม่ใช้ write set เก่าเป็นข้อห้ามงาน Master

**Acceptance:** Resume ไม่ยอมรับผลเมื่อ relevant inputs เปลี่ยนและ skip ผลที่ตรงจริง; ขั้น plan-only ตรวจ schema/path เท่านั้น; full release verifier รอ frozen PR/release SHA

## ตรวจและบันทึกปัญหา

`tool/cli/tests/r15-scope.tests.ps1`, `tool/cli/tests/verify-scope.tests.ps1`

Verification kind: `cli-contract`; bounded subsystem: `Runtime`. ยืนยัน targets กับ actual reconciled source ก่อนรัน. ใช้ existing passed evidence เมื่อ source/dependency/config fingerprint ยังตรง; ไม่รัน full release verifier ก่อน frozen PR/release SHA และไม่รัน heavy suites พร้อมกัน. Docs/source-inspection ใช้ hash/path/link/Git/diff checks ไม่สร้าง Flutter PASS แทน. Planned targets ไม่ใช่ผลทดสอบที่รันแล้ว

บันทึก unexpected failure ตั้งแต่ครั้งแรก: DefectID, source/config, steps, expected/actual, evidence, root cause, fix และ retest. คำสั่ง/filesystemล้มซ้ำหรือไม่มี progress10นาทีให้หยุดวิธีที่ผิดแล้ว diagnose ไม่ retry วน. ใช้ judgment แก้ recoverable blockers; external-required ที่ยังไม่ผ่านเป็น NOT RUN/external-pending. ก่อนปิด package ตรวจ diff/callers/data integrity และ acceptance ใหม่โดยแยกจาก implementation pass

## สถานะประวัติ

ใช้acceptedHistory/handoffเดิมตรวจย้อนหลัง. ไม่มีdispatchauthorityจากpackageนี้ งานที่เหลือใช้20bundlesตามworkflow ไม่เขียนทับevidenceเก่า
