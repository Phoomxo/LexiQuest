# LexiQuest · G7.5 · Activation matrix และ edition ที่ซื่อสัตย์

Revision `2026-09-13-sequential-3` · 53/64 · internal package `P7.5` · model `gpt-6-astra` · thinking `medium`

**Authority:** ผู้ใช้อนุมัติ “เริ่ม G0.1 แล้วส่งต่อทีละ task” และแก้รหัสเป็น G0.1–G8.9. ทำ package นี้ให้ผ่านแล้วสร้าง successor หนึ่งตัวตาม index; ใช้วิจารณญาณแก้ปัญหาที่จำเป็น. ถ้ามีคำสั่งหยุดใหม่ให้หยุดตามนั้น. ไม่ใช้ subagents/background implementation workers

## รับงานและอ่านเฉพาะที่เกี่ยวข้อง

อ่าน `C:/Users/Phet/.codex/visualizations/2026/09/13/01a09888-61dd-7680-9b19-c33035043d59/full-system-orchestration/handoffs/G7.4.json` และ run-state ก่อนเขียน source. ใช้ `acceptedSourceSha`, branch/worktree, content/config/model pins และ evidence จาก handoff ที่มีอยู่จริง. Native task worktree อาจเริ่มจาก default branchที่เก่ากว่า; ตรวจ Git common directory/HEAD/dirty/collision แล้วรับ accepted source อย่างปลอดภัยตาม workflow. ใช้เอกสาร revision ที่ successor รับจาก accepted commit ไม่ทับด้วย seed7712เก่า

อ่าน [AGENTS](../../../../AGENTS.md), [Active Index](../../../development/full-system-active-index.md), [Rule Register](../../../development/2026-09-13-rule-supersession-register.md), [Sequential Workflow](../../../development/full-system-package-workflow.md), [Master Plan](../../plans/2026-09-13-lexiquest-full-system-master-plan.md) เฉพาะหัวข้อ `P7.5`/phase gate/related formulas และ [work ledger](../../../development/2026-09-13-full-system-work-ledger.json) เฉพาะ requirements/coverage ของตน. ข้อความ scope/acceptance ด้านล่างสรุปจาก Master revisionนี้; ถ้า master เปลี่ยนให้ reconcile version ก่อนใช้

ก่อนเริ่มตรวจ task/package/receipt และ current writer จาก run-state; claim writer ตาม workflow เมื่อ source พร้อม. Dependency requirements: P7.4. ลำดับ execution ใช้ previous G7.4 แม้ requirement dependency จะมีรายการอื่น

## ขอบเขตและผลที่ต้องได้

ทดสอบ off/unavailable/implementedOff/internal/enabled/killswitch แยก; ตรวจทุก catalogentry ใน localpreviewedition และการเปิด f12/f23 ตาม design; f28research ยังต้อง authority จริง

**Requirements:** COV-f12, COV-f23, COV-f28, COV-f41, G7

**Owner paths เริ่มต้น:** `lib/runtime/production_feature_contract.dart`, `lib/runtime/production_feature_gate.dart`, `lib/runtime/app_dependencies.dart`, `lib/features/learning/application/lesson_mode_registry.dart`. ขยายไฟล์ที่จำเป็นได้พร้อมเหตุผล/consumer/compatibility; ไม่ใช้ write set เก่าเป็นข้อห้ามงาน Master

**Acceptance:** matrix แสดง route+deps+content+flag+edition ครบ; feature ที่ยังปิดมี explicitlimitation; ไม่อ้าง 44runtime ครบเมื่อบางรายการเป็น externalpending

## ตรวจและบันทึกปัญหา

`test/runtime/runtime_feature_controls_test.dart`, `test/features/learning/lesson_mode_registry_test.dart`, `test/scenarios/runtime_kill_switch_journey_test.dart`

Verification kind: `targeted`; bounded subsystem: `Learning`. ยืนยัน targets กับ actual reconciled source ก่อนรัน. ใช้ existing passed evidence เมื่อ source/dependency/config fingerprint ยังตรง; ไม่รัน full release verifier ก่อน frozen PR/release SHA และไม่รัน heavy suites พร้อมกัน. Docs/source-inspection ใช้ hash/path/link/Git/diff checks ไม่สร้าง Flutter PASS แทน. Planned targets ไม่ใช่ผลทดสอบที่รันแล้ว

Candidate bounded commands จาก ledger (ตรวจ actual source/CLI contract ก่อนเลือก):

- `& .\tool\cli\verify-scope.ps1 -Level Targeted -Area Learning -TestTargets @('test/runtime/runtime_feature_controls_test.dart','test/features/learning/lesson_mode_registry_test.dart','test/scenarios/runtime_kill_switch_journey_test.dart') -Resume`

บันทึก unexpected failure ตั้งแต่ครั้งแรก: DefectID, source/config, steps, expected/actual, evidence, root cause, fix และ retest. คำสั่ง/filesystemล้มซ้ำหรือไม่มี progress10นาทีให้หยุดวิธีที่ผิดแล้ว diagnose ไม่ retry วน. ใช้ judgment แก้ recoverable blockers; external-required ที่ยังไม่ผ่านเป็น NOT RUN/external-pending. ก่อนปิด package ตรวจ diff/callers/data integrity และ acceptance ใหม่โดยแยกจาก implementation pass

## ส่งต่อและจบ task

เขียน compact checkpoint ใน `docs/development/full-system/checkpoints/G7.5.md` พร้อม source/requirements/changed paths/verification/defects/decisions/remaining gates. Commit เฉพาะ accepted files หลังตรวจ staged diff; ห้ามย้าย unrelated dirty files หรือทิ้ง user data. บันทึก actual full SHA ใน `C:/Users/Phet/.codex/visualizations/2026/09/13/01a09888-61dd-7680-9b19-c33035043d59/full-system-orchestration/handoffs/G7.5.json` **หลัง commit** ตาม mandatory fields ใน workflow แล้ว release application writer. เก็บหลักฐานให้ successorอ่านได้ ไม่ใส่ PASS/accepted จากการมี test fileหรือจากคำสั่งที่ยังไม่รัน

เมื่อ acceptance ผ่านและ execution ยัง authorized ให้ reserve และสร้าง **G7.6 เพียงหนึ่ง task** ด้วย exact entry ใน [Task Index](../../../development/full-system-task-index.json). เรียก list_projects ก่อน create_thread; Git projectใช้ native worktree default, `model: gpt-6-astra`, `thinking: medium`. Prompt ใส่ accepted source SHA, own handoff/control/next brief paths และ authorization ที่ต่อเนื่อง. ตรวจ duplicate/uncertain createจาก receipts; บันทึก actual IDs และ wait progressหนึ่งครั้งตาม workflow แล้ว emit created-thread directive. หยุด source writes ของ taskนี้หลัง handoff

ไม่ fork ประวัติทั้ง task และไม่สร้าง64tasksล่วงหน้า. Future task เริ่มจาก own brief + accepted handoff เพื่อจำกัด context โดยรักษา evidence/requirementsครบ
