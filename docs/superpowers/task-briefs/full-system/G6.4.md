# G6.4 · Speech gateways และ fallback

Revision `2026-09-13-bundles-4` · requirement `P6.4` · 46/64 · `gpt-6-astra` / `medium`

Bundle `B14` · ข้อ1/3ในtaskเดียว · **ห้ามdispatchจากpackageนี้**

## รับ requirement เมื่อถึงลำดับ

อ่าน [bundle brief](../../bundle-briefs/B14.md) และcurrentPackageId/package receiptsตาม [workflow](../../../development/full-system-package-workflow.md). เมื่อถึง `G6.4` จึงอ่านMaster `P6.4`/relatedledger/callers/tests. Previous `G6.3` เป็นdependency/order; ภายในbundleรับผลย่อยในworktreeเดิม ไม่สร้างtaskหรือรอold handoffs/Gx.y.json. ตอนเริ่มbundleรับacceptedpredecessorSHAจริง

อ่าน [AGENTS](../../../../AGENTS.md), [Active Index](../../../development/full-system-active-index.md), [Rule Register](../../../development/2026-09-13-rule-supersession-register.md), [Master](../../plans/2026-09-13-lexiquest-full-system-master-plan.md) และ [ledger](../../../development/2026-09-13-full-system-work-ledger.json) เฉพาะส่วนที่เกี่ยวข้อง. Technical scope/acceptance/testsด้านล่างคงจากacceptedG0.5โดยไม่ลดเกณฑ์

## ขอบเขตและผลที่ต้องได้

ตรวจ permissions/no-speech/unavailable recognizer/timeout/cancel/error/retry/startcompletion, TTS/noaudio และ dictationfallback; ไม่แก้ sharedspeechpolicy จาก screen-onlyworkaround โดยไม่ตรวจ consumer

**Requirements:** A-SYS, VOICE-01, MODE-dictation, MODE-speaking, MODE-shadowing, G6

**Owner paths เริ่มต้น:** `lib/features/media_practice`, `lib/screens/speak_to_text_screen.dart`, `lib/screens/shadowing_challenge_screen.dart`. ขยายไฟล์ที่จำเป็นได้พร้อมเหตุผล/consumer/compatibility; ไม่ใช้ write set เก่าเป็นข้อห้ามงาน Master

**Acceptance:** ไม่มี lateassessment หลังหน้าจอแสดง failure; text/manualpractice ยังได้; inputlocks ตรง session และไม่บันทึกเสียงจริงโดยอนุมาน

## ตรวจและบันทึกปัญหา

`test/features/media_practice/speech_practice_use_cases_test.dart`, `test/features/media_practice/plugin_speech_recognition_gateway_test.dart`, `test/screens/shadowing_challenge_screen_test.dart`

Verification kind: `targeted`; bounded subsystem: `AI`. ยืนยัน targets กับ actual reconciled source ก่อนรัน. ใช้ existing passed evidence เมื่อ source/dependency/config fingerprint ยังตรง; ไม่รัน full release verifier ก่อน frozen PR/release SHA และไม่รัน heavy suites พร้อมกัน. Docs/source-inspection ใช้ hash/path/link/Git/diff checks ไม่สร้าง Flutter PASS แทน. Planned targets ไม่ใช่ผลทดสอบที่รันแล้ว

Candidate bounded commands จาก ledger (ตรวจ actual source/CLI contract ก่อนเลือก):

- `& .\tool\cli\verify-scope.ps1 -Level Targeted -Area AI -TestTargets @('test/features/media_practice/speech_practice_use_cases_test.dart','test/features/media_practice/plugin_speech_recognition_gateway_test.dart','test/screens/shadowing_challenge_screen_test.dart') -Resume`

บันทึก unexpected failure ตั้งแต่ครั้งแรก: DefectID, source/config, steps, expected/actual, evidence, root cause, fix และ retest. คำสั่ง/filesystemล้มซ้ำหรือไม่มี progress10นาทีให้หยุดวิธีที่ผิดแล้ว diagnose ไม่ retry วน. ใช้ judgment แก้ recoverable blockers; external-required ที่ยังไม่ผ่านเป็น NOT RUN/external-pending. ก่อนปิด package ตรวจ diff/callers/data integrity และ acceptance ใหม่โดยแยกจาก implementation pass

## บันทึกผลย่อยและทำข้อต่อไป

อัปเดต `docs/development/full-system/bundles/B14.md` ซึ่งเป็นMarkdownหนึ่งไฟล์ต่อbundle; รายละเอียดอยู่ `docs/development/full-system/evidence/bundles/B14` และ external `packages/G6.4.json`. ตรวจ/commitacceptedsub-resultแล้วบันทึกSHAจริงหลังcommit คงbundlewriterและทำข้อต่อไปในtaskเดิม. nextPackageเป็นลำดับไม่ใช่dispatch

เมื่อทุกpackageของ `B14` ผ่านเท่านั้น จึงปิดbundlehandoff/releasewriterและให้workflowส่งต่อ nextBundle `B15`. ห้ามสร้างtaskจากbriefนี้ ไม่สร้างMDรายงานต่อpackage/commit. รักษาevidence/NOT RUNตามจริง
