# G0.8 · ตกลง edition และปิด gate เริ่มพัฒนา

Revision `2026-09-13-bundles-4` · requirement `P0.8` · 8/64 · `gpt-6-astra` / `medium`

Bundle `B01` · ข้อ3/3ในtaskเดียว · **ห้ามdispatchจากpackageนี้**

## รับ requirement เมื่อถึงลำดับ

อ่าน [bundle brief](../../bundle-briefs/B01.md) และcurrentPackageId/package receiptsตาม [workflow](../../../development/full-system-package-workflow.md). เมื่อถึง `G0.8` จึงอ่านMaster `P0.8`/relatedledger/callers/tests. Previous `G0.7` เป็นdependency/order; ภายในbundleรับผลย่อยในworktreeเดิม ไม่สร้างtaskหรือรอold handoffs/Gx.y.json. ตอนเริ่มbundleรับacceptedpredecessorSHAจริง

อ่าน [AGENTS](../../../../AGENTS.md), [Active Index](../../../development/full-system-active-index.md), [Rule Register](../../../development/2026-09-13-rule-supersession-register.md), [Master](../../plans/2026-09-13-lexiquest-full-system-master-plan.md) และ [ledger](../../../development/2026-09-13-full-system-work-ledger.json) เฉพาะส่วนที่เกี่ยวข้อง. Technical scope/acceptance/testsด้านล่างคงจากacceptedG0.5โดยไม่ลดเกณฑ์

## ขอบเขตและผลที่ต้องได้

กำหนด local preview edition, target device, content availability และ activation matrix; ยืนยัน f23 ที่ bootstrap เปิด internal เมื่อ learningPreviewEnabled และตัดสิน f12 ที่ยัง implementedOff; f28 คง research fail-closed และแสดงข้อจำกัดจนมี personal-assessment decision ที่ไม่เปลี่ยน research authority

**Requirements:** COV-f12, COV-f23, COV-f28, COV-f41, G0

**Owner paths เริ่มต้น:** `lib/runtime/app_dependencies.dart`, `lib/features/learning/application/lesson_mode_registry.dart`, `lib/features/time_tracking/application/focus_timer_rollout.dart`. ขยายไฟล์ที่จำเป็นได้พร้อมเหตุผล/consumer/compatibility; ไม่ใช้ write set เก่าเป็นข้อห้ามงาน Master

**Acceptance:** G0 ปิดได้เมื่อ source ownership/authority conflict/coverage assignment/cleanup disposition พร้อม; ผู้ใช้ทราบกรณี edition ยังไม่ครบ 44 runtime; ไม่มีการเปิด cloud/research/paid/model rollout จาก gate นี้

## ตรวจและบันทึกปัญหา

`test/runtime/runtime_feature_controls_test.dart`, `test/features/learning/lesson_mode_registry_test.dart`

Verification kind: `delivery-review`; bounded subsystem: `Runtime`. ยืนยัน targets กับ actual reconciled source ก่อนรัน. ใช้ existing passed evidence เมื่อ source/dependency/config fingerprint ยังตรง; ไม่รัน full release verifier ก่อน frozen PR/release SHA และไม่รัน heavy suites พร้อมกัน. Docs/source-inspection ใช้ hash/path/link/Git/diff checks ไม่สร้าง Flutter PASS แทน. Planned targets ไม่ใช่ผลทดสอบที่รันแล้ว

บันทึก unexpected failure ตั้งแต่ครั้งแรก: DefectID, source/config, steps, expected/actual, evidence, root cause, fix และ retest. คำสั่ง/filesystemล้มซ้ำหรือไม่มี progress10นาทีให้หยุดวิธีที่ผิดแล้ว diagnose ไม่ retry วน. ใช้ judgment แก้ recoverable blockers; external-required ที่ยังไม่ผ่านเป็น NOT RUN/external-pending. ก่อนปิด package ตรวจ diff/callers/data integrity และ acceptance ใหม่โดยแยกจาก implementation pass

## บันทึกผลย่อยและทำข้อต่อไป

อัปเดต `docs/development/full-system/bundles/B01.md` ซึ่งเป็นMarkdownหนึ่งไฟล์ต่อbundle; รายละเอียดอยู่ `docs/development/full-system/evidence/bundles/B01` และ external `packages/G0.8.json`. ตรวจ/commitacceptedsub-resultแล้วบันทึกSHAจริงหลังcommit คงbundlewriterและทำข้อต่อไปในtaskเดิม. nextPackageเป็นลำดับไม่ใช่dispatch

เมื่อทุกpackageของ `B01` ผ่านเท่านั้น จึงปิดbundlehandoff/releasewriterและให้workflowส่งต่อ nextBundle `B02`. ห้ามสร้างtaskจากbriefนี้ ไม่สร้างMDรายงานต่อpackage/commit. รักษาevidence/NOT RUNตามจริง
