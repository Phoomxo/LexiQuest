# Stabilization 1–6 Implementation Plan

**Goal:** Execute the user's six priorities on the existing 02fa worktree, preserving user data and all inherited changes.
**Architecture:** Repair the existing host endurance lifecycle; keep canonical learning/export authorities and native test oracles. Separate terminal test failure from a running workload requiring cancellation.
**Tech stack:** Flutter/Dart, Python host runners, Android vivo, existing synthetic fixtures.

## Constraints

- Branch codex/pair-matching-pm0-pm8, HEAD 788e90e6; desktop Documents checkout is not the current implementation.
- User explicitly authorized items 1–6 on 2026-09-10, including recovery of the previously failed test. Preserve the old failed journal. Never uninstall/clear app data or touch unowned files.
- No deployment, real research, paid services or feature activation from items 7–12.
- Snapshot each touched file before editing; serialize Flutter/build/device workloads.

## Sequential deliverables

- [x] 1. Repair `build/verification/motivation-ui-20260908/endurance-fixture-runner.py` and its `_test.py`: test terminal response identity, sentinel, actual test failure, process and driver completion before settling a failed workload. All success oracles remain unchanged. Run `python build/verification/motivation-ui-20260908/endurance-fixture-runner_test.py`. Reconcile old UUID/PID/APK/archive against the read-only audit before exact owned recovery and compatible APK restoration; record fresh evidence. The later disconnected run was reconciled on 2026-09-11 using its exact fixture hash, UUID and owner markers; 287 archived synthetic files were removed and the compatible manual APK hash was verified.
- [ ] 2. Discover literal internal values in participant screens with `rg`; replace only presentation labels (including balancedGrowth) with Thai mappings and test the affected screens. Preserve serialized identifiers.
- [ ] 3. Extend the existing native export Save/readback/provider-delete workflow to PDF, Anki and JSON. Validate actual bytes and format contents, render PDF for visual inspection. Keep evidence separate per format.
- [x] 4. Six-minute smoke passed. On 2026-09-10 the user accepted the observed **2h48m28s** endurance evidence for this iteration instead of requiring a fresh 180-minute run. Preserve the failed machine journal: battery exhaustion prevented final phase/cleanup verification, so this is user acceptance of partial-duration evidence, not a 180-minute test PASS. Device recovery and temporary logging cleanup remain pending reconnect.
- [x] 5. Freeze source; run current full regression, analyzer and applicable local policy gates; build ordinary test APK and record version, SHA256 and limitations. No publishing.
- [x] 6. Inspect TalkBack availability and actual focus/labels/navigation on vivo. Fix proven accessibility defects with maintained semantic tests. If source changes, rerun affected checks and rebuild the delivered APK. Do not equate semantic tests with human spoken-output UAT.

## Evidence

Save this task's incremental snapshots, logs and recovery journal under `build/verification/stabilization-1-6-20260910/`. Update a concise maintained checkpoint with exact outcomes and remaining dependencies.
