# Review Remediation Implementation Plan

> **For agentic workers:** Use subagent-driven-development task-by-task. One implementation writer; root runs Flutter/Dart/build/codegen serially. User already authorized implementation and testing; continue through all tasks without a new permission gate.

**Goal:** แก้ findings R01–R29 จาก review และตรวจระบบหลังรวมการแก้ โดยแยก local completion จาก device/human/external acceptance

**Architecture:** ใช้ learning/reward/evidence/owner authorities เดิม ซ่อมการประกอบ UI, durable state และ transport boundaries เพิ่ม migration เท่าที่จำเป็นโดยจองเวอร์ชันก่อนเขียน ไม่ลดข้อบังคับของข้อมูลเพื่อทำให้ test ผ่าน

**Tech Stack:** Flutter/Dart/Drift, Android, Python/FastAPI, PowerShell, existing Firestore/Supabase policy fixtures

## Global constraints

- Worktree `C:/Users/Phet/.codex/worktrees/02fa/LexiQuest`; branch `codex/pair-matching-pm0-pm8`; starting HEAD `788e90e62b1694c20945734787723c168b6a6ab2`
- Preserve inherited dirty/untracked work. Baseline copies/hashes: `build/verification/review-remediation-20260909/baseline`; 1,603 files. Do not use HEAD-only diff to attribute edits to this task.
- Specification: `docs/superpowers/specs/2026-09-09-review-remediation-design.md`; findings/RED evidence: `docs/development/2026-09-09-whole-system-code-review.md` and `build/verification/code-review-20260909/`.
- Preserve 8/44 feature catalog, frozen EvidenceContext/EventEnvelopeV2 and canonical authorities. Starting schema24/48 tables; Task4 source is moving to reserved25 with48tables. Inspect the active ledger/code before reserving any subsequent version.
- No real credentials/participants, permission prompts on the user's device without coordination, data clear/uninstall, paid model training, deployment, or real research upload.
- Tests first. Reuse the review reproductions where meaningful; promote durable regression into existing suites. Do not weaken assertions or fabricate completion/score/authority.
- Root runs toolchains; agents write tests then stop for RED evidence before production edits. Review and focused GREEN must pass before the next implementation task. Read-only planning in disjoint modules may overlap.
- Execution refinement after D: while E's maintained tests are being prepared, root may close the independent single-file Task7 download repair against its verified RED; E production stays frozen until explicit writer handback. No overlapping production writers or toolchains. Advance tests for Tasks5/8 also use disjoint files and their original pre-edit baselines. This changes execution order, not the required per-task review or final full-system gate.
- For each task root saves a baseline-relative diff; independent reviewer returns spec compliance and code quality. No commit is necessary to snapshot a task, and no inherited edits are staged indiscriminately.
- For later tasks touching a file already repaired by an earlier task, capture a sparse before-task overlay before its writer starts (`build/verification/review-remediation-20260909/snapshot_task.py`). Use `make_diff.py --overlay <name>-baseline` for that task's incremental review; final whole-task attribution still uses the original1,603-file baseline. Never overwrite a snapshot or use a post-edit snapshot as RED/source-before evidence.
- Ledger: `docs/development/2026-09-09-review-remediation-results.md`; root alone updates plan/ledger. Detailed task decisions are appended before that task's implementation when source discovery resolves interfaces.

## Task 1: Finish the actual review lesson (R01)

**Files:** `lib/screens/main_navigation_screen.dart`, existing `lib/screens/quiz_screen.dart` and `lib/features/learning/presentation/unified_lesson_shell.dart` only if their existing-session seam needs extension; `test/screens/main_navigation_screen_test.dart`, `test/screens/review_center_screen_test.dart`, relevant existing quiz/shell tests.

**Interfaces:** Consumes ReviewCenter's attached `ReviewLessonLaunchRequest.session` and canonical learning identity. Produces an actionable lesson on the same session, durable answer/complete and result navigation. Do not call a second start method simply to reuse QuizScreen.

- [x] Promote the actual navigation fixture from `build/verification/code-review-20260909/review_route_test.dart` into maintained tests; replace the broad control-count assertion with a real learner response and durable terminal-state assertions.
- [x] Root reproduce RED through Today→Review→Start, using synthetic reviewed content. Existing diagnostic assertion is `expect(actions, findsWidgets)` within the lesson body, excluding the back button; stronger acceptance must assert one session, one answer per occurrence and completed state.
- [x] Inspect existing quiz controller/session attachment, then implement a renderer bound to the attached session. Reuse existing feedback/result components and normal Thai UI. If an additional renderer is necessary, discover equivalents first and record its exact path in the task report before writing it.
- [x] Test correct/incorrect answer, completion retry, exit/back, route feature-off and reopen. Verify that completion produces SRS/ledger effects allowed by the existing evidence policy and no duplicate session.
- [x] Root focused tests and independent spec+quality review; save diff/evidence and mark R01 closed only when the full response path works.

Task1 acceptance found a related source gap: a single-item attached review has only the correct choice because the existing adapter derives its pool solely from session questions. Verified RED `task-1-distractor-red.log` seeds a second real owner-scoped vocabulary row but cannot find its wrong option. Extend the existing learning use-case read boundary with a bounded owner-pinned distractor loader and the meaning adapter with a separate immutable candidate pool; `QuizScreen` uses this only for attached review. No extra session questions/attempts, no fabricated content, no recognition/SRS policy change. Validate owner before/after await, freeze the pool for the renderer, retain equivalent-answer filtering, and show an honest insufficient-content state if no real alternative exists. Added scope: `learning_use_cases.dart`, `meaning_quiz_mode_adapter.dart` and focused use-case/adapter tests. Actual route must test correct then reopened wrong, one learning event per attempt, and no SRS under the existing recognition policy.

## Task 2: Speech finalization and cloud playback (R03–R05)

**Files:** `lib/features/media_practice/application/speech_practice_use_cases.dart`, `lib/screens/speak_to_text_screen.dart`, `lib/screens/shadowing_challenge_screen.dart`, `lib/voice/synthesized_voice_route_handler.dart`, existing player/provider interfaces and their tests discovered from imports.

- [x] Add maintained regressions for final callback during stop, partial→noMatch with no final score, and synthesized playback waiting for real completion. Review probes are `review_reproductions_test.dart`, `shadowing_derived_test.dart`, `cloud_voice_derived_test.dart`.
- [x] Root run RED; separate accepting finalization from cancel/retirement; propagate actual completion futures from player through cached/fresh synthesis.
- [x] Verify stop/cancel/final races, owner/route retirement, player failure/cache/cancellation and no duplicate evidence; root run media/voice/screen tests and review diff.

Task2 source notes: `VoiceAudioPlayer.play` and `AudioPlayerAdapter.playBytes` currently signal start only; production uses audioplayers. Supply a genuine completion signal subscribed before playback begins, and distinguish natural end from stop/dispose/error. Preserve compatibility for existing injected players with an optional completion-capable interface if appropriate; a completed `play()` Future is not audio completion. Capture the per-play future for both cache and fresh synthesis. Speech stop must admit one final callback from the current attempt, including callbacks delivered around stop completion, while cancellation/new start/owner takeover retire old attempts; the two screen epoch increments must be corrected too. Shadowing may show live transcript but only a final event can produce the assessment/evidence.

Verified installed dependency detail: speech_to_text7.4.0 `_stop()` awaits platform stop and then schedules its final-result timer; `_onFinalTimeout()` can emit the final result later. A fix admitting only callbacks inside `gateway.stop()` still fails on the production timing. Test final after stop acknowledgment through use case and both screens. An injected audio adapter without completion support must leave proof unavailable; never substitute `Future.value()` as natural end. Native completion streams need safe error/closed-stream handling, and stop/dispose failures must retire pending proof without claiming silence.

Broad-suite refinement: existing speaking UX intentionally permits a valid same-attempt final after a recoverable engine error, clears that recognition error and preserves unrelated playback errors. Do not blanket-retire every failure. Terminal noMatch, cancellation, release and takeover retire delayed-final acceptance; engine recovery stays compatible. Keep plugin cleanup after accepted final, even when logical result delivery has ended. Tests must exercise the actual AudioplayersAdapter with an injected controlled AudioPlayer stream; fake optional-adapter tests alone do not verify production stream close/error/stop-failure behavior.

Task2b integration follow-up: Task3a's broader learning suite exposed two existing sentence-practice panel regressions after the preceding media-only gates passed. An accepted queued native stop must survive logical release while waiting for delayed native start; preserve entry admission and post-stop callback acceptance, but do not drop already-accepted cleanup merely because the old attempt retired. Add delayed-start/final-cleared-flag/release and cross-owner ordering regressions in the existing speech use-case suite, retain both sentence-panel tests, and verify the media/voice/screens plus sentence panel before re-closing Task2. This bounded follow-up runs with reading writer frozen, then Task3a resumes; no concurrent production writers.

## Task 3: Reading, progress, recommendations and calendar (R06–R09,R11,R28)

R06 details: `docs/superpowers/specs/2026-09-09-reading-repair-details.md`. Use existing canonical checkpoint recovery; no reading migration or new suspend feature. This task may be executed as sequential3a reading /3b progress-calendar subphases with separate review because the files and acceptance are independent.

**Files:** `lib/screens/associative_reading_session_screen.dart`, its checkpoint/repository interfaces if required, `lib/features/progress/data/drift_progress_queries.dart`, `lib/features/learning/domain/srs_policy.dart`, `lib/runtime/app_bootstrap.dart`, `lib/features/goals/application/learning_goal_use_cases.dart`; corresponding tests discovered before editing.

- [x] Add resume-after-recall summary regression linked to the original durable evidence; do not relabel old answers as belonging to a new session. Task3a R06 closed with independent review and final relevant1231pass; Task3b closure is recorded below.
- [x] Promote `data_reproductions_test.dart` mapping/deleted-due cases and SRS long-sequence case. Add runtime-style recommendation availability test and DST-independent calendar-date arithmetic test.
- [x] Root run RED. Correct skill mapping using current prompt types, actionable due predicates, bounded SRS intervals, complete mode availability and calendar day arithmetic.
- [x] Verify current adapters through progress, archived/deleted/shared words, long correct/incorrect/replay sequences, reading preference with feature off, and resumed summary/owner isolation. Root GREEN and review. Task3b final relevant1706pass plus independent13-file spec/quality PASS; no migration or device claim.

Task3b read-only discovery: progress `_skills` must retain legacy modes and add canonical `pronunciationTranscript`, `typedRecall`, `associativeRecall` producers. The due-count predicate must exclude deleted words/categories while retaining historical SRS rows and allowing exact packaged starter content; inspect ReviewCenter quality/manifest eligibility before declaring count actionable. SRS growth must saturate before multiplication/Duration construction; choose and document a finite interval cap, inspect algorithm-version replay/sync handling before any version change, and test existing oversized snapshots plus repeated correct/reset sequences. Bootstrap currently enumerates ActiveRecallLadder only, excluding supported reading recommendations; derive availability from the recommendation service's supported modes and current feature authority, including initialization/feature-off transitions. Countdown should subtract UTC date-only ordinals of the timezone policy's learning-day Y/M/D, following existing streak logic, without changing timezone authority. Tests belong in the existing progress_projector, srs_policy, app_bootstrap and learning_goal_use_cases suites; no migration expected.

Task3b bounded-policy decision: use a named maximum interval of36,500days as a numerical safety ceiling, preserving current1/3/7/14/doubling behavior below the ceiling. This is not a claim of an educationally validated100-year review recommendation. Saturate the prior interval before multiplying; incorrect recall still resets to1day. Mark newly computed/rebuilt snapshots algorithmVersion2 because capped results differ from version1; do not relabel stored version1 caches or rewrite accepted attempts. Current projection rebuild already recomputes from immutable evidence with the injected policy, and sync persists an integer algorithm version or rebuilds when local evidence exists. The implementer must inspect remaining version assertions and include a deterministic rebuild test before closure; no eager migration or bulk update is authorized by this decision.

## Task 4: Durable quest periods and expiry (R02,R10)

Detailed decisions: `docs/superpowers/specs/2026-09-09-quest-repair-details.md`.

Migration execution refinement: `build/verification/review-remediation-20260909/task-4-migration-mechanics-review.md` records installed Drift2.34.3 behavior, FK-OFF-before-transaction requirement, child/history validation, idempotent latest-layout recovery and the raw v24 fixture construction. Follow these verified mechanics. Local synthetic migration cases have now run; current results and unresolved gates are in the authoritative result ledger.

Pre-implementation catalog-history check confirmed that recurring assignments make v1 terminal history followed by v2 catalog replacement reachable. The detailed spec now requires a bounded immutable per-instance definition snapshot in schema25 (still48 tables), honest migrationCatalog/legacyUnknown backfill, historical receipt recovery from the pinned grant rather than the current catalog, and cross-version regression. Existing valid receipts remain authoritative. Include this refinement in the Task4 brief before dispatch; no additional table or reward authority.

**Files:** `lib/data/local/tables/quest_tables.dart`, `lib/data/local/app_database.dart`, generated Drift output through generator, `docs/database/schema_ledger.md`, quest domain/use cases/repository, `lib/features/identity/data/drift_owner_upgrade_repository.dart`, export and schema fixtures affected by the actual migration.

- [x] Complete period/backfill design from the current schema and reserve version25 only after checking the ledger remains free; list exact migration/index/backfill and owner-merge semantics before production edits.
- [x] Add RED next-day renewal and expiry tests from `quest_verified_test.dart`, then migration-with-history, same-day no duplicate reward, day boundary, replay and owner-merge tests.
- [x] Preserve terminal instance/reward identity. Replace all-state owner/quest uniqueness with appropriate period/active constraints and persist sufficient timezone/period identity. Expiry uses event time for replay eligibility.
- [x] Once recurring histories are possible, cover recovery beyond64 instances; the previously unreachable LIMIT64-before-source-filter path must be made correct as part of this migration.
- [x] Root codegen serialized, migration/quest/identity/export/deletion tests; inspect foreign keys and current schema export. Independent review before closing both findings. All bundles reviewed; integrated1662pass/1timezone-fixture failure followed by corrected complete Pair file9pass, with production and other test inputs unchanged. No claim that the earlier broad command exited0; full repository regression remains Task12.

## Task 5: Honest reminders and owner transition (R12–R14)

**Files:** `lib/screens/study_reminder_settings_screen.dart`, `lib/features/reminders/data/platform_reminder_scheduler.dart`, reminder use cases/repository, `lib/runtime/app_bootstrap.dart` and owner coordinator only where needed for the canonical lease.

- [x] Add RED pending-retry→reopen UI, native grant→OS revoke via method-channel mock, and independent worker reconciliation during owner transition. Existing probes: `notification_permission_test.dart`, `reminder_owner_boundary_test.dart`.
- [x] Model display status from actual desired/platform state; known OS denial overrides cached grant. Fence source cleanup through transition using canonical lease, preserve rollback and sweep captured platform IDs after completion.
- [x] Test cancellation failures, feature epoch changes, lease expiry, operation failure, old/new owner, and bootstrap worker entry. Final focused16/full369pass, independent16-file spec/quality PASS and matching hashes. Includes bounded current-state repair after already-issued stale native cancellation; no physical notification UAT claim.

Task5 source notes: reuse the existing `coordinateOwnerErasure` pattern (begin canonical-token fence, capture platform IDs before row changes, cancel, operation, final sweep, finally end fence), but first verify lease ordering. Bootstrap currently wraps `UpgradeGuestOwner` in reminder coordination outside the repository operation; that operation may acquire the lease internally. Do not fabricate a token, acquire a competing nested lease or merely add an in-memory lock. Resolve an existing-owner-lease seam through the canonical owner coordinator/repository and test the actual bootstrap caller. Distinct repository instances already reproduce the worker race.

Task5 approved lease seam: add `OwnerTransitionLifecycle.run<T>({required String sourceOwnerId, required String operationToken, required Future<T> Function() operation, required String Function(T) targetOwnerId})` to existing identity/domain/owner_upgrade.dart, optional injection on DriftOwnerUpgradeRepository. Invoke only inside its actually acquired lease, outside SQLite transactions, for upgrade/logout/rollback; source lookup and research preflight precede native cancellation or secret mutation. Rollback resolver returns the previous owner for void. Bootstrap composes the reminder adapter and removes only outer reminder wrappers, preserving canonical learning coordination. coordinateOwnerChange receives the actual token and checks canonical ownership around native awaits; marker checks alone are insufficient. No worker acquires a nested lease while holding the reminder serializer.

Task5 failure semantics: begin exact-token fence and capture union of row IDs and decoded native actual platform IDs. Put partial pre-cancel inside failure handling; abort mutation on failure and restore source desired state only with retained authority after ending its fence. Final captured-ID sweep must stop on token loss or owner takeover. Preserve operation exceptions and durable committed results independently from cleanup, heartbeat or release failures. A fresh reconciliation must discover decoded inactive-owner native orphans even when source rows/outbox were removed, feature is off or permission denied. Cancel entry.platformId after payload/owner revalidation, never a recomputed ID; unknown foreign payloads are not reminders. Failed cancellation stays discoverable and reports degraded. Acceptance includes all3transitions, partial precancel, exact-token takeover, actual bootstrap/second-connection worker ordering and restart convergence. Read-only preparation and exact test files are recorded in task-5-lease-orphan-review.md and task-5-orphan-tests.md; no migration expected.

Task5 status/authority APIs: `StudyReminderRepository.isOwnerOperationTokenOwned(operationToken, nowUtc)` delegates to the canonical gate rather than interpreting fence markers. Read-only `StudyReminderUseCases.loadStatus({required expectedOwnerId, required StudyReminderSource source})` returns `StudyReminderStatusSnapshot` in existing study_reminder.dart containing the reminder and `StudyReminderDisplayStatus` disabled/scheduled/pendingRetry/permissionDenied/unavailable/elapsed. Scheduled requires matching current desired revision, native entry and permissions with no unresolved platform intent; elapsed never claims delivery. Loading/resuming/rebinding the screen must not prompt, schedule or mutate rows. Preserve owner, feature epoch and desired revision checks across awaits; old callbacks cannot overwrite replacement/disposed UI. Bound writer set is9existing production files (identity domain/repository, reminder model/domain repository/Drift repository/platform scheduler/use cases, screen and bootstrap), with corresponding maintained tests. Capture Task5 overlay after Task4 integration before edits; ORIGINAL override the3advance reminder tests.

Task5 read-only integration refinement is saved in `build/verification/review-remediation-20260909/task-5-preparation.md`. The canonical seam is `_withOwnerOperationGate`'s existing acquired token callback: inject an optional transition lifecycle wrapper inside that lease and outside the transaction for upgrade/logout/rollback; remove only outer reminder coordination in bootstrap and retain learning coordination. Capture/cancel/final-sweep exact old IDs, end fence before restoration/reconciliation, preserve durable operation result when postcommit cleanup fails, and retain lease ownership checks through awaits. Persistent orphaned native notifications need an explicit later-reconcile repair or existing durable cleanup intent; an in-memory sweep alone is not durable retry. Settings should derive current scheduling truth from desired intent, actual permission and native entries rather than isEnabled/cache alone. No automatic permission prompt or new migration assumed.

Independent lease/orphan verification: `build/verification/review-remediation-20260909/task-5-lease-orphan-review.md` records exact callbacks, actual native platform-ID handling, partial precancel restoration, token-loss/same-owner takeover fencing, heartbeat/release result preservation and serializer/lease lock ordering. Native decoded pending payload supplies restart retry after source rows disappear; existing row-bound outbox alone cannot. Include these acceptance cases plus read-only settings refresh on resume before closure.

## Task 6: Owner-safe bookmark and research restore/withdrawal (R15–R17)

Detailed decisions: `docs/superpowers/specs/2026-09-09-sync-repair-details.md`.

Execution refinement: with Task5B production frozen for additional tests, the isolated pure proof codec may be implemented as Task6B0 before StageA persistence. It has72maintained missing-API cases (`task-6-proof-expanded-api-red.log`), no database or transport dependency, no migration/reservation. Only the new domain codec is production-owned during this interval;5B remains tests-only until codec freeze and explicit writer handback. Root owns all toolchains. Reserve26 only when the schema stage actually starts after ledger verification. Task6 baseline captures overlapping prior work; ORIGINAL override advance sync/consent/proof/reopen tests where verified.

**Files:** `lib/features/sync/data/drift_sync_store.dart`, `lib/features/consent/data/drift_research_consent_repository.dart`, `lib/features/sync/data/drift_research_sync_adapter.dart`, `lib/features/research/data/drift_research_sync_authorizer.dart`, associated sync contracts/policy tests only if the verified transport design requires them.

StageB execution refinement after schema26: root59 migration cases and251 database/legacy lifecycle cases passed, with independent core7-file review. Close the small proof lifecycle slice before B2 source preparation so any newly prepared proof already has export and withdrawal handling: ResearchLifecycleExportReader, owner lifecycle manifest, consent suppression and explicit upgrade inventory only. Its maintained11case RED is4pass/7fail; B1 deletion/upgrade positive controls already pass. The exact14-field personal export contract is recorded in the sync spec. One production writer remains mandatory; B2 source preparation and B3 receiving/rules follow after this slice is verified. This sequencing changes no participant authority or learning workflow.

- [x] Write exact transport design before edits: bookmark local ID can differ from stable wire ID; withdrawal intent survives reaccept; session/Pair purpose authority needed by opportunities and mission events must be authentic and restoreable. Detailed contract/migration/pin decisions are recorded in sync-repair-details.md; pure codec74pass with independent2-file PASS. StageA and migration/transport remain open.
- [x] Complete maintained reproduction coverage through the actual accepted-session/fresh-receiver fixture, including pull, owner/permit tampering, exact tombstone replay and conflicting facts. Receiver22PASS; the missing historical `mission_single_test.dart` was not claimed executed.
- [x] Implement owner-scoped local saved IDs without overwriting historical items; commit withdrawal intent atomically with withdrawal; claim exact deny intent despite later consent changes. StageA final1296pass, actual emulator denial2pass, independent10-file spec/quality PASS and matching hashes; real file reopen and two-client gateway simulation covered with explicit policy-modeling limits.
- [x] Supply missing restore authority through the verified versioned proof seam; B2 source36/broad413PASS and B3 receiver22PASS cover legacy rows, ordering, incomplete retry and immutable conflicts without creating learning/reward authority.
- [x] Root integration5580PASS, actual Firestore175PASS/Auth3PASS, independent corrected client6/server2 review plus integrated data review. Research remains default-off; no deployed policy or enrollment claim.

## Task 7: Completed partial model recovery (R18)

**Files:** `lib/features/device_model/application/model_download_manager.dart`, corresponding model download tests.

- [x] Add RED complete `.partial` with HTTP416 using `model_derived_test.dart`; assert verification/activation without downloading beyond EOF.
- [x] Verify existing complete bytes before range request, preserving checksum/identity/cancel checks. Test short/oversized/corrupt files and interruption before rename.

Task7 source preflight: the existing `_performDownloadAndActivateLocked` already shares SHA/interpreter verification and activation after streaming. Prefer skipping only the HTTP/open-write block when partial length equals expectedBytes, then use that same verification path; never accept size alone. Cancellation is currently checked before HTTP and during chunks but not after verifier await. Cover cancellation during verification before rename/activation, especially the new complete-partial path with no chunk loop. Preserve file-lock, manifest/hash checks, existing cached-final handling and single activation receipt; no real model download is needed for synthetic tests.
- [x] Root full model subsystem tests and review.

## Task 8: Backend request boundaries and deployment contract (R19–R22)

Execution refinement after Task4/9 closure: the independent backend writer may implement this verified RED bundle while Task5's writer prepares tests only. One production writer remains mandatory; Task5 production waits until backend freeze/review handback. Root runs all test commands serially. Existing AI/Voice app modules may contain bounded private ASGI middleware classes to avoid introducing an unconfigured cross-package dependency; discover equivalents first. Final backend diff must override ORIGINAL bytes for advance test_app.py files, test_models.py and the originally absent test_llm_content_service.py.

**Files:** AI/Voice `app.py` and local test suites, `backend/ai_api/src/lexiquest_ai/services/llm_content_service.py`, `backend/ai_api/src/lexiquest_ai/config.py`, `backend/lexiquest_lm/scripts/DEPLOY.md` and relevant service contract docs.

- [x] Add RED real ASGI streamed-body size tests for both services, urllib Retry-After preservation, short/long config canaries; reuse isolated review probe inputs.
- [x] Limit consumed bytes before parse, return413 without invoking handler/provider; preserve valid requests and disconnect behavior. Preserve headers including retry timing; hide validation inputs without weakening URL validation.
- [x] Correct deployment topology to content API→LM contract with exact existing environment variables discovered from Settings; do not claim direct `/v1/content` support in LM Space.
- [x] Document authentication honestly: the content service currently supplies a configured static provider key, while the LM Space verifies an expiring Firebase ID token. A topology diagram or base URL change alone is not a working long-lived bridge; do not put a permanent client token in configuration or claim this integration ready. Describe the supported local provider path and the missing refresh-capable authenticated bridge as a deployment dependency unless an existing implementation is discovered. Remove unsupported blanket cost/timing guarantees from the touched deployment passage.
- [x] Root backend tests using frozen existing virtualenvs, including unauthenticated oversized request and synthetic integration; review source/errors for secret leaks. Full AI106pass, Voice74pass/1pre-existing external opt-in E2E skip; independent10-file spec/quality PASS. External Firebase/OmniVoice integration remains unverified.

## Task 9: Collector-to-signer contract (R23)

Execution refinement: root may close this isolated CLI repair while E's remaining lifecycle fixture is being diagnosed with production frozen. Root remains the only production writer and serializes all test commands. Actual PowerShell5.1 suite155pass/1missing-helper failure versus7.6.5 suite153pass/3fail confirms an additional JSON reader compatibility defect: automatic DateTime conversion changes signed timestamp bytes. Add one pipeline JSON reader in the existing evidence library, selecting ConvertFrom-Json -DateKind String when that parameter exists and ordinary parsing otherwise. Use it at the receipt/source, signer, verifier, collector manifest and evidence-assembly JSON boundaries; preserve exact original strings, strict UTC/signature/release matching, no DateTime-to-string normalization of already altered evidence. Add exact nested timestamp/signing-byte roundtrip acceptance and run both actual executable versions. These bounded CLI readers extend Task9 scope; no certificate-store, signing, collection or release action is authorized.

Approved pure boundary: `Test-LexiQuestDeviceCollectorEnvelope -DeviceEvidence -ReleaseManifest` in existing `tool/cli/lib/field-release-evidence.ps1`, consumed by the actual signer in place of its initial binding condition. Return bool only; nested physical must be actual bool true, required collector/release pins must exist and match, malformed structures return false. Keep all subsequent receipt, signature and trusted-key validation intact. Maintained collector-shaped fixtures remain explicitly synthetic; an absent-helper API RED is not physical-device evidence or an executed signer/certificate test.

Independent review clarified malformed scalar handling: all text pins and collector origin must be actual nonempty strings, both versionCode values actual integral values, with exact equality. Reject arrays/objects/coerced numeric strings before comparison. Negative fixtures must use the preserving JSON reader and a roundtrip positive control so PowerShell timestamp coercion cannot falsely satisfy another field's rejection test.

**Files:** `tool/cli/sign-android-field-evidence-result.ps1`, `tool/cli/tests/verify-field-release.tests.ps1`, existing collector schema fixture.

- [x] Add synthetic producer→consumer validation that fails for the current incorrect physical-property access without reading real certificate/private-key material.
- [x] Validate nested `device.physical` and other consumed envelope fields before signing. Preserve signature verification/field evidence requirements.
- [x] Root local PowerShell tests and script syntax checks; review exact contract diff. Final202pass on actual5.1 and7.6.5,7scripts parse with0errors, independent7-file spec/quality PASS. No real signing/collection claim.

## Task 10: Training data and evaluation identity (R24–R26)

Execution refinement: with Task5A frozen and independently reviewed, implement this isolated CPU-only bundle while Task5B prepares tests only. Task10 is the sole production writer until its freeze. Verified expanded RED is22pass/15behavior failures (`task-10-final-red.log`). Pronoun retains coarse function classification but selects existing universal `any` sentence templates, avoiding the function family's preposition slots. Validate an explicitly requested adapter directory before heavy imports; always invoke the adapter loader afterward so disappearance cannot silently evaluate base under an adapter label. No downloads, training or dependency changes.

Read-only implementation preparation: `build/verification/review-remediation-20260909/task-10-preparation.md` records the actual seed-union boundary, missing-adapter import/load ordering, maintained test locations and CPU-only constraints.

**Files:** `backend/lexiquest_lm/dataset/enrich_dictionary.py`, `backend/lexiquest_lm/dataset/generate_synthetic.py`, `backend/lexiquest_lm/train/evaluate.py`, existing LM dataset/evaluate tests.

- [x] Promote three synthetic cases from `lm_pipeline_probes.py` into maintained tests. No network/model downloads needed.
- [x] Merge seed union before enrich with curated fields winning; resolve Pronoun before substring Noun; reject explicitly missing adapter before base/model loading and report exact loaded artifact.
- [x] Root dataset/train/evaluate suite and controlled pipeline round trip. Preserve explicit base-only evaluation and deterministic generator seed. Focused37/fullCPU94pass;three-word CLI pipeline16rows/12-2-2splits/dry-run-stub PASS, independent root6-file review and matching hashes.

## Task 11: Retry and unknown usage UI (R27,R29)

Root source inspection also found the owner lifecycle archive's `aiUsageDiagnostics` aggregate independently uses `SUM(COALESCE(total_tokens,0))`. Apply the same complete/known/count contract there and extend its existing allowed export fields in owner_lifecycle_manifest.dart. Add maintained archive unknown/mixed/measured-zero acceptance before implementation; retain record counts, provider/model sanitization and cost completeness. This is aggregate export consistency within R29, not a raw-event export change or migration. Bound source set extends to owner_lifecycle_archive.dart and its manifest, with focused archive/scenario tests.

Approved summary contract: nullable `AiUsageSummary.totalTokens` means a complete aggregate only; required `knownTokens` is the sum of explicitly reported row.totalTokens, and required `tokenReportedRequestCount` counts terminal requests with nonnull reported totals (including measured0). totalTokens is present only when this count equals requestCount. Preserve existing request/outcome semantics including failure/indeterminate metadata and excluding pending; do not infer totals from partial input/output or change cost completeness. Keep raw event export untouched; aggregate export includes the known subtotal/count and nullable total. Thai UI distinguishes unknown, explicitly zero and partially reported totals without estimates or literal null. Before changing an inherited modified/untracked test, compare ORIGINAL baseline bytes rather than relying on git status.

Read-only implementation preparation: `build/verification/review-remediation-20260909/task-11-preparation.md` records stream identity/retry lifecycle, existing category test coverage and nullable-token aggregation/UI semantics.

**Files:** `lib/screens/categories_page.dart`, `lib/features/ai_tutor/data/drift_ai_usage_repository.dart`, usage summary domain and `lib/screens/ai_tutor_settings_screen.dart`; relevant existing tests.

- [x] Add RED category stream error then successful retry and usage success without token metadata plus mixed known/unknown totals.
- [x] Bind retry to a fresh load/subscription; preserve unknown/partial token statistics through domain and UI with plain Thai copy.
- [x] Root category/usage/AI settings tests and review. Core36/archive17pass;broaderAI-vocabulary-export176pass;independent10-file spec/quality PASS and matching hashes. Raw event storage/cost semantics unchanged, no migration.

## Task 12: Integrated verification and acceptance status

Final dependency refinement (2026-09-10): actual OSV found six newly reported HTTPX2/HTTPCore2 advisories at2.9.1 in both AI and Voice development groups. Publisher changelog and PyPI identify2.12.0 as covering the reported fixes. Update only these two explicit dev pins and regenerate their locks through uv with targeted upgrades; preserve unrelated runtime/GPU pins, synchronize only CPU/dev environments, rerun real backend tests and exact-lock scans. No new vulnerability ignores or expiry extensions. A separate newly reported Accelerate checkpoint-index traversal/DoS advisory has no published fixed version; inspect its optional training/Voice and Space attribution, retain explicit unresolved status and disabled/unreleased remote ML paths rather than claiming a clean scan or installing/training a GPU stack. This does not block ordinary local app/native verification.

Read-only discovery: `build/verification/review-remediation-20260909/task-12-preparation.md` records existing gate commands, reviewed native harness locations, stale evidence and physical/emulator build distinctions; inspect exact source state before executing.

Task12 integration refinement: the real Dart CLI final-plan check failed with dart:ui unavailable after research_sync began importing the proof codec's existing Pair semantic dependencies. The generator consumes only the rules revision metadata. Move that single authoritative constant into existing pure sync_entity.dart beside the other rules revision constants, re-export it from research_sync.dart for source compatibility, and remove the generator's unnecessary heavy import. Preserve actual Pair validation; do not replace its decoder or substitute a Flutter-only invocation for the documented Dart CLI. Re-run the real generator/architecture contracts, regenerate artifacts normally if current schema/rules/source digests drift, then full regression. Four analyzer warnings (unused import/redundant non-null assertions) also receive semantics-neutral correction before final freeze.

Native acceptance refinement after fresh authorized/unlocked vivo discovery: complete the bounded ignored harness changes before fixture builds. Source manifests must fingerprint only the explicitly reviewed empty generated-file deletion and reject other missing inputs. Endurance/UI nonparticipant invariants must include schema26 proof rows. Extend existing remediation UI fixture with a separate actual app-text200/reduced-motion scenario, observed enabled menu destinations and intermediate control captures; preserve real viewport, synthetic owner, disabled cloud/AI, exact source/pin/APK/restore chain. Root runs all builds/device operations serially. Disjoint ignored harness writers may work while the full host regression sees frozen production/maintained tests; their source-manifest delta is explicitly separate and all native inputs freeze before builds. Keep normal and accessibility receipts separate, inspect actual PNGs, then native export/profile and smoke before three3600-second endurance phases. No user-data clearing or historical-schema restore.

- [ ] Freeze production writers; compare final diff against the 1,603-file baseline and inspect every changed/new file. Run final independent cross-subsystem review and fix actionable findings.
- [ ] Root format/analyze current source, full Flutter regression, backend suites, Firestore/Supabase policy tests and generated feature-plan contracts. Record meaningful case counts separately from loader/hooks and retain actual failures.
- [ ] Build/identify/archive debug APK from that exact source; verify package/signature/hash. Prepare release artifact only if its required signing authority is available; do not publish.
- [ ] Read-only discover ADB. If device usable, run approved non-destructive native routes, content/export save/readback, current profile, UI coverage and short endurance before full180-minute real workload. Keep original budgets and source/device evidence.
- [ ] Before native fixture installation/restoration, verify compatibility with the final forward-only database schema. A historical v24 binary cannot restore an app whose real database was upgraded to25 or later; use a verified compatible final-source predecessor and preserve user data. Never force downgrade or clear data as recovery.
- [ ] Record screen coverage against the requested28-page list; do not equate widget tests with visual/device acceptance. Keep human voice/camera/TalkBack outcomes pending if user participation is necessary.
- [ ] Update the result ledger with all29 dispositions, exact pass/fail/not-run, source/APK identity, active processes and next executable external checks. No blanket completion claim while required local work remains.
