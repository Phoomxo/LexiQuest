# E3.3 / F04 — Use the word

Accepted local implementation. Base `b7a7c8fbee8d41e53a13fc867772a7185e20efe6`, branch `lexiquest/post-g83-e33`, writer `01a0bc95-ef50-7522-9d96-69cf11161f58`, claimed under exclusive lock/CAS at revision 256. Implementation and sequential succession authorized by external `post-g83-v3-implementation-authorization.json`. Standard/default requested, effective tier unverified. No subagents.

Objective: turn an exact saved personal-set sense into a written prompt with separate meaning/use/form feedback and immutable revisions. Reuse F01 identity, content admission and owner lifecycle. Schema 32 adds only supplemental rubric history; frozen EvidenceContext/EventEnvelope, SRS, rewards, research and outbox stay unchanged.

Design tradeoff: a finite, local response catalog for the three original book/pencil/bottle senses provides reproducible assessed examples without pretending to grade arbitrary language. Unrecognized sentences remain uncertain, including valid alternatives outside the reviewed catalog. This is source/AI-assisted engineering review, not independent human calibration, general writing assessment or efficacy evidence. No provider is invoked. Default rollout remains off; internal UI acceptance is required.

Worked example: “I read a book.” and “She reads a book.” each demonstrate the requested meaning/use/form. “She reads book.” demonstrates meaning/use but needs a determiner. “She read a book every day.” may describe past habitual reading and must not be penalized as a present-tense error. An unfamiliar valid sentence receives uncertainty, not a false incorrect result.

Completion benchmark: reviewed rubric fixtures, real SQLite restart/revision/idempotency, migration/restore, guest merge/export/delete, owner A→B→A, cancellation/content withdrawal/retirement, real set-to-writing UI at narrow text200 layout, affected regression and self-review all pass on pinned inputs. No E4.1 dispatch until this benchmark is met.

Evidence: **414 tests / 26 targeted files PASS**, fingerprint `7ba95ffd19fce0d1c848b2bb0534814bd081401f101c99cda3f6f04ba87df607`. Static analysis: zero errors/warnings, two unchanged bootstrap infos. The [acceptance receipt](post-g83-e33/acceptance.json) pins source files and the external raw-run archive. [Rubric review](post-g83-e33/rubric-review.json) documents 15 assessed and eight uncertain fixtures. [Self-review and defects](post-g83-e33/defects-and-review.json) records corrections. [Rendered result](post-g83-e33/writing-result-320-text200.png) shows the real set-to-writing journey at 320px/text200.

Storage is additive schema32, now56 named tables. SQL protects immutable result payloads; owner-only remapping remains available. Real SQLite tests cover restart, same-operation replay, changed-payload/stale-revision rejection, cancellation before and after insertion with rollback, guest transfer/export/delete, exact backup restore, malformed/future/cross-owner rejection, and migrations from28/31 with interrupted-upgrade recovery. Local writing backup includes private responses and requires exact personal-set revisions restored first; privacy export exposes counts only. Backup/restore remains usable with practice rollout off. Neither is a claim of complete database archive restoration.

Internal runtime tests exercised quiz/studyPlanning retirement and durable owner-generation changes. Default-off is asserted. Canonical attempts/SRS/points/outbox remain empty for writing; original evidence codecs and assets are unchanged. Existing rehearsal/context, owner-lifecycle, plan storage and frozen migration data regressions passed. E6 integrated review/freeze and E7/G8.4–G8.9 remain required; no physical-device, provider, release or deployment claim.

Diagnosed corrections: writer claim initially raced the predecessor's dispatch receipt; re-read revision255 and claimed256 without overwriting it. First red run ended with InputDrift after implementation began before verifier finalization; no valid gate claim from that run. Owner-switch fixture now advances the real durable generation. Widget fixture uses real async for file I/O and bounded readiness polling; lifecycle privacy is checked after resume because paused Flutter frames are disabled. Rubric review corrected a valid past-tense sentence before acceptance. No assertions skipped or defects transferred.
