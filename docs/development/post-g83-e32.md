# E3.2 / F06 contextual vocabulary

Accepted from E3.1 `0244314a2bd9a3588ff968f21a075c48da9c9c64` on `lexiquest/post-g83-e32`. Exact resulting commit is pinned in the external E3.2 handoff.

Objective: practice exact personal-set contexts, explain the canonical answer, and resume without creating a second answer or review. The existing cloze, owner, checkpoint, evidence and guided-repair authorities remain responsible; no new scoring engine, scheduler or database schema was introduced.

Three contexts (book, pencil and bottle) bind the original starter sense, lexical artifact, sentence, explicit alternative and both rationales. The cup distractor is verified too; ambiguous reverse cup/bottle and unsupported contexts are rejected. Original12 scored meanings and all source assets remain unchanged. This is source/AI-assisted engineering review, not independent human review or learning efficacy validation.

Worked example: “I read a book with fifty pages.” supports **book**; **pencil** is a writing tool, not a collection of pages. Selected answers remain recognition. Typed input is pinned for the whole session; neither format switching nor hints precede the canonical answer. After a wrong selection the existing guided repair records assistance separately. Typed answers receive explanations without inventing a selected-distractor repair ticket.

Tradeoff: explicit inventory coverage is small but auditable. The personal-set chooser exposes selected/typed context practice and restart only in the internal rollout; production defaults off. Resume authenticates saved order/sense/artifact, checkpoint version, attempt sequence and wrong-choice provenance, and reopens the last canonical explanation. It does not restore raw typed text or claim general archive restoration.

Completion benchmark passed: 399 tests across 15 targeted files, stable pre/post source fingerprint, real SQLite restart, exactly-once review, source withdrawal, unsupported checkpoints, owner A–B–A, guest merge/export/delete, research-off isolation and runtime feature retirement. The reachable Thai UI was captured at 320px and 200% text and visually inspected. Analysis has zero errors/warnings and the same six informational findings reproduced on predecessor source. Review is writer self-review; all identified in-scope defects were corrected and retested.

[Acceptance and source pins](post-g83-e32/acceptance.json), [defect/review record](post-g83-e32/defects-and-review.json), and [UI evidence](post-g83-e32/context-why-320-text200.png). Detailed red and green runs are preserved externally under orchestration `evidence/E3.2/all-runs`.

Recovery used corrected literal-path discovery, fixture initialization and provenance validation after failures; no defects were skipped or assertions weakened. No subagents, deployment, new costs, research activation, live provider, device/camera or full release claims. Standard/default requested; effective tier unverified. E3.3 is next; E6 and every E7/G8.4–G8.9 gate remain required.
