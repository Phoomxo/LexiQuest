# Device remediation — approved sequential execution

User approved continuous execution on 2026-09-07. Worktree: `02fa/LexiQuest`, branch `codex/pair-matching-pm0-pm8`, baseline HEAD `788e90e62b1694c20945734787723c168b6a6ab2`.

1. Reproduce the media navigation and configured Pair Retry failures with bounded vivo tests. Distinguish production behavior from live-binding/viewport artifacts. Keep the original assertions and exact saved-session identity checks.
2. Make the smallest evidence-backed correction, verify on host and vivo, and retain failed baseline evidence. Run device fixtures in independent app processes when a shared live binding contaminates error zones.
3. Split Adventure frame measurements into build and raster times. Fix the measured cause while preserving the approved p95 budget of 16.7 ms and all authority checks. Validate the original profile fixture on the actual device viewport.
4. Run affected regression, analysis, and the integration checkpoint against a recorded current source snapshot. Historical frozen-source results do not certify subsequent edits.
5. Complete remaining native camera/microphone and accessibility/UAT checks with real user participation where required. Remote Voice requires its existing external test authorization/token. Record unavailable prerequisites accurately.
6. Update the evidence report and readiness decision. Keep release, research enrollment/upload, and G4P sign-off separate from engineering results.

Device install uses the existing signer and `adb install -r -t`; restore and hash-check the backed-up original APK after each run. Never uninstall or clear app data. Serialize Flutter builds/tests and preserve unrelated workspace changes.
