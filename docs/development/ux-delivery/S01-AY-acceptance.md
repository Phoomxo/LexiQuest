# S01-AY — existing explicit personal word form recovery

Status: HOST_PASS (host only); see evidence/S01-AY-validation.json and S01-AY.md. Original source hypotheses below are resolved by the recorded tests. One existing AddWordScreen create/edit journey, not new instruction or content.

Read AGENTS.md, README/state/latest handoff, S01.md, ready-backlog.md, readiness-12-groups.md, AX validation/reassessment and this card. Verify COMPLETE exact-base overlay; validate raw path segments, Windows reserved names including superscript COM/LPT variants, containment, casefold uniqueness, exact ZIP membership/CRC/every size/hash/deletion. Copy manifest separately. Preserve ignored media/evidence and ALL original archives, including inherited XY/Z checkpoint source-overlay.zip and every predecessor-changed-bytes.zip. Only the new transport's self-reference is excluded. Predecessor immutable. Persist source receipt and sole-writer claim with ACTUAL CODEX_THREAD_ID, then reconcile local LIVE ACTIVE writer/worktree/feature branch/currentItem/new revision AND stale nextAction/checkpoint/continuation/dispatch/nextDispatch before messaging claim or application edits.

## Concrete scope

AddWordScreen binds the owner only once, initializes controllers from the initial word, retains direct field/CEFR/optional fill/save callbacks, and messages ordinary errors using mounted/feature admission. Native save omits displayed expectedOwnerId. updateWord checks owner/admission only before canonical mutation; createWord already has optional transaction admission. These are hypotheses, not confirmed AY RED.

Confirm meaningful widget/route/isolated-Drift RED before fixes: dependency/selected category or word replacement/removal, canonical owner change, partial draft privacy, retained fill/CEFR/save/cancel controls, explicit early Save during initial owner read, read failure and Thai retry, duplicate writes, uncertain acknowledgement, read-only committed reconciliation, route/tab/pop/disposal/lifecycle/emergency-off-on retirement, and any in-flight update boundary. Owned current form must remain usable; retain partial draft on recoverable read errors. Recheck actual owner/category/word snapshots, lexical identity/local revision and outbox authority. Explicit-only writes; no automatic resubmission. Already committed writes are not revoked. A canonical owner rejection is stale context, not invalid text. Only a fresh owner-checked read proving unchanged source can re-enable an explicit retry after ambiguous failure. Preserve create/edit distinctions and existing category/import/delete behavior; use existing authorities, no parallel mutation authority.

No new content/media/difficulty/instruction/activity/completion/scoring/session/event/learning writers, assignment/scheduling/statistics/research/schema/wire protocol/provider work. Isolated DB/provider/voice doubles only, no user-data import/call/upload. Baseline offline without login/AI; qualified content/media/trial gates remain. Native/device/real restart/keyboard/screen-reader/visual/user/trial/release pending; UX-D01–25 OPEN; original S01 start/due unchanged.

## Execution and recovery

One writer/package, no subagents/background workers. Sequential bounded tool/cli/verify-scope.ps1 exact discovered targets, full release only frozen SHA. Preserve completed result/streams immediately because same-selection verifier envelopes overwrite. Compare canonical path/hash maps across Learning/Runtime. No source edits/concurrent heavy checks during gates or rerun of passed unchanged closures. Preserve RED/final source ZIPs and all inherited originals. Reassess all twelve groups and continue useful ready plan-v5 work automatically. At justified context boundary preserve COMPLETE immutable source/evidence, release writer, then dispatch exactly one saved-project successor and reuse its ID.

Stop failed METHOD, discover/diagnose/correct/retest automatically. Repeated failures or ten minutes without progress require a NEW diagnostic approach, NEVER abandonment/hidden RED/weakened assertions/writer release to bypass gates. Stop only explicit pause or concrete indispensable external prerequisite with no useful independent work. Use rg --files before exact paths; no Windows wildcard file arguments. The current implementation and sequential succession authorization supersedes old analysis-only limits; preserve the external recovery-policy.json interpretation.

Astra/medium deterministic fallback, not Jev-selected; Standard/default requested/runtime tier unexposed. Original Jev USD5 ledger/quota/cache unchanged. Fresh executor/free-credit/auto-reload/shared-spend/quota evidence and durable reservation before live routing; no resets/purchases/pending clearance. Existing controller login block and heartbeat unchanged; no repeat probes. No Security/Codex Security/Security Scan/Deep Scan, paid provider, deployment or browser rejection bypass. Thai updates; no repeat approval.

## AX diagnostics to retain

535 final host tests PASS, 32 new cases; 27 meaningful original-source RED and three introduced regressions resolved. Source closure 1,552 pins, 157 inherited ZIPs intact. Core 65 tests/warnings5; widget102/warnings4; related84/warnings0; runtime284/warnings25. Warnings are separate memory fixtures or inherited; none suppressed. Prototype115+3 reused on eight unchanged pins. Self-review only.

AX owns deletion in WordDeleteDialog and a single-flight parent. GuardedVocabularyWordDeletionRepository optionally wraps the original canonical delete body with active-owner and expected word/category checks before commit. Uncertain deletion is never automatically retried; exact unchanged word readback permits another explicit try. Optional committed deletion may reconcile read-only after cover/return while old writes stay retired. All category/create/edit/import bodies remain unchanged after line-ending normalization. No remaining AX RED.

UTF-8 stdout/files explicit; verifier streams can be UTF-16 BOM/mojibake Thai. TestName is --plain-name, not regex. Initial Flutter setup may change environment fingerprint with unchanged source; preserve/diagnose InputDrift. Never await void handleAppLifecycleStateChanged. Observe synchronous/immediate Future errors immediately; context-dependent initialization belongs after dependencies exist. Explicit early Save must await its initial owner read and then revalidate, not silently drop the user press.

Real SQL streams need bounded real IO plus fake frames (runAsync 10ms + pump100ms fifteen times), with disposal/stream-close draining and DB closure inside widget zone. Do not initialize serialized DB/owner setup in runAsync. Existing file-backed fixtures may explicitly run IO as already structured. AX corrected delete-reconciliation/offline-journey fixtures to wait actual dialog/read completion and assert no LinearProgressIndicator. Legacy post-commit read fault gate only activates after mutationCalls>0; otherwise preflight reads can stall before the intended commit assertion. One diagnostic stopped only verified f934 tester/compiler/parent processes; final groups completed. Preserve those original fixtures/diagnostics and all data/outbox/session assertions. RawTooltip is not IconButton; bind actual icon control/identity and assert hitTestable after scrolling. AW/AT cached asset/owner/fixture diagnostics remain in earlier handoffs.
