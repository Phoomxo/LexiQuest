# S01-AZ — existing explicit manual personal import recovery

Status: HOST_PASS596 for the bounded host implementation; see S01-AZ.md and evidence/S01-AZ-validation.json. Native/user/trial/release pending. Original source hypotheses below are retained as acceptance history. One existing AddMultipleWordsScreen journey, not new content or instruction.

Read AGENTS.md, README/state/latest handoff, S01.md, ready-backlog.md, readiness-12-groups.md, AY validation/reassessment and this card. Verify COMPLETE exact-base overlay with raw path segments, Windows reserved names including superscript COM/LPT, containment, casefold uniqueness, exact ZIP membership/CRC/every size/hash/deletion. Copy manifest separately. Preserve ignored media/evidence and ALL original archives including XY/Z checkpoint source-overlay.zip and every predecessor-changed-bytes.zip. Only the new transport self-reference is excluded. Predecessor immutable. Persist source receipt and sole-writer claim with ACTUAL CODEX_THREAD_ID, then reconcile local LIVE ACTIVE writer/worktree/unused feature branch/currentItem/new revision and stale nextAction/checkpoint/continuation/dispatch/nextDispatch before messaging claim or application edits.

## Concrete scope

AddMultipleWordsScreen binds owner once, retains fill/preview/import callbacks, omits displayed expectedOwnerId for native import and uses mounted-only completion/error messages. ImportVocabulary preserves row validation/sourceHash/import identity and forwards cancellation; DriftVocabularyImportRepository checks cancellation during its transaction. Inspect actual final-row/outbox/receipt and active-owner/category boundaries before proposing any changes. These are source hypotheses, not confirmed AZ RED.

Confirm meaningful widget/route/isolated-Drift RED for dependency/selected-category replacement or removal, canonical owner changes, pending owner admission and explicit early Import, partial-draft privacy and read failure/Thai retry, retained fill/preview/import/cancel/back callbacks, duplicate submits, uncertain acknowledgement and read-only committed reconciliation, route/tab/pop/disposal/lifecycle/emergency-off-on retirement, and any in-flight import boundary. Keep the current owned form usable. Preserve actual import ID/sourceHash, per-row failures and truncation, duplicate detection, category limits, owner/category/word/outbox/receipt authority. Check actual source snapshots before retry; explicit-only writes, no automatic resubmission or revoking committed imports. Canonical owner rejection is stale context, not malformed text. Reconciliation must recheck owner after pending reads. Use existing mutation authority, no parallel store or schema/wire protocol.

No new content/media/difficulty/instruction/activity/completion/scoring/session/event/learning writers, assignment/scheduling/statistics/research/provider scope. Isolated DB/provider/voice doubles only, no user-data import/call/upload. Preserve existing file import, category/create/edit/delete and CEFR behavior. Baseline offline without login/AI; qualified content/media/trial gates remain. Native/device/real restart/keyboard/screen-reader/visual/user/trial/release pending; UX-D01–25 OPEN; original S01 dates unchanged.

## Execution and recovery

One writer/package, no subagents/background implementers. Sequential bounded tool/cli/verify-scope.ps1 with exact discovered targets; full release only frozen SHA. Preserve completed result/streams immediately because same-selection envelopes overwrite. Compare canonical path/hash maps across Learning/Runtime. No source edits/concurrent heavy checks during gates or reruns of passed unchanged closures. Preserve RED/final source ZIPs and every inherited original. Reassess all twelve groups and continue useful ready plan-v5 work automatically. At justified context boundary preserve COMPLETE immutable source/evidence, release writer, dispatch exactly one saved-project successor and reuse its ID.

Mandatory recovery interpretation and CURRENT IMPLEMENTATION AUTHORIZATION: plan-v5 implementation and sequential saved-project succession are authorized; older analysis-only limits superseded. Stop failed METHOD, discover real paths/symbols, diagnose, correct and retest automatically. Repeated failures or ten minutes without progress require a NEW diagnostic approach, NEVER abandonment, hidden RED, weakened assertions, fabricated PASS or releasing writer to bypass acceptance. Stop only explicit user pause or concrete indispensable external prerequisite with no useful independent authorized work. Preserve unresolved exact checkpoints; use rg --files and literal existing paths, not Windows wildcard file arguments.

Astra/medium deterministic fallback, NOT Jev-selected. Standard/default requested/runtime tier unexposed. Original Jev USD5 ledger/quota/cache/login block/heartbeat unchanged. Fresh executor/free-credit/auto-reload/shared-spend/quota evidence and durable reservation before live routing; no reset/purchase/pending clearance. No repeat login probes, Security/Codex Security/Security Scan/Deep Scan, deployment, paid provider or browser rejection bypass. Thai updates; no repeat approval for authorized work.

## AY diagnostics to retain

561 final host tests PASS;26 new cases,23 meaningful original-source RED and4 candidate regressions resolved. All1,554 path/hash pins current across four gates. All171 inherited original ZIPs unchanged. Core91/warnings7;widget102/warnings4;related84/warnings0;runtime284/warnings25. Warnings are separate-memory fixtures/inherited, none suppressed. Prototype115+3 reused on8 unchanged pins. Scoped static analysis clean; self-review only. No AY RED remains.

Canonical create/update/delete/category bodies unchanged after line-ending normalization. Optional GuardedVocabularyWordUpdateRepository wraps existing update with owner/expected word/category and transaction admission. Native create now opts into the preexisting create admission with category snapshot. Known committed word saves reconcile read-only; ambiguous failure requires fresh unchanged-source read before separate explicit retry.

Do not retire native callbacks merely on ordinary rebuild. Keep BackButton semantics. DropdownButton calls onTap AFTER pushing its PopupRoute: distinguish the owned CEFR picker from external cover. Existing private edit text stays hidden until initial owner admission; explicit early create Save awaits its owner read. Keep these tests and assertions.

UTF-8 stdout/files explicit; verifier streams may be UTF-16 BOM/mojibake Thai. TestName is --plain-name, not regex. Initial setup can cause environment-only InputDrift; preserve/diagnose it. Never await void handleAppLifecycleStateChanged. Observe synchronous/immediate Future errors immediately. Context-dependent initialization belongs after dependencies exist.

Real SQL streams need bounded real IO + fake frames (runAsync10ms then pump100ms fifteen times), disposal/stream-close draining and DB closure inside widget zone. Avoid putting a whole stream-dependent command inside runAsync; drive the Future with bounded IO/frames. AY trace showed commands had finished but runAsync DB close after disposal stalled; three ownership-verified local tester stops and original diagnostics/fixtures are retained. Final gates completed normally. Two legacy route doubles now include their actual selected category; assertions unchanged. Prior AX post-commit fault gates apply only after mutationCalls>0; retain original row/outbox/restart/session checks and all earlier diagnostics.
