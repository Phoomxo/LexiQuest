# S01-AA — Native four-tab entry for existing features

Status: SOURCE PREFLIGHT COMPLETE; RED tests and implementation NOT STARTED. Authority: controller/user continuous plan-v5 execution instruction 2026-09-24; §§15.3 R2-03 and 15.5 permit independent navigation. Pending usability/native review does not block implementing existing navigation. New instructional units still require WP3/14.3 evidence. One writer; no subagents or parallel heavy gates.

## User outcome and observed discrepancy

Normally available baseline opens วันนี้ / ฝึก / คำศัพท์ / ฉัน. Users can start existing practice without login/AI and reach progress/mastery, achievements, quests/shop/history/settings through sensible secondary locations. Current `lib/screens/main_navigation_screen.dart` `_buildEntries` (163–219 at preflight) has learning/vocabulary/mastery/achievements/profile; `NavigationGlossary` labels home/learn เรียน; home/today is secondary push. This source mismatch is concrete; no widget RED has been run yet.

## Existing contract findings to preserve

- `MainNavigationScreen.initialIndex` currently defaults to 0 and explicitly indexes *visible* legacy destinations (learning, vocabulary, mastery, achievements, profile), with clamping. Production `/home` in `lib/navigation/app_route_factory.dart` calls the default constructor. `AppEntryStateStore` persists guest/signedOut only, not a tab. Search found no production caller supplying initialIndex; tests and profile-owner lifecycle use explicit indexes, including 99. Preserve explicit historical entry intent. One possible compatible design distinguishes omitted entry (Today) from explicit legacy index, maps legacy secondary intents to a parent plus guarded named route; decide after tests, do not silently reinterpret old index 1 as Practice.
- Existing stable IDs (home/learn, home/today, home/vocabulary, home/mastery, home/achievements, home/profile and children) remain stable. Glossary primary/secondary membership and Thai labels may change intentionally. Do not expand supported external deep links merely because internal IDs exist; AppRouteFactory currently supports a limited external route set.
- `_buildTodayHub` (997+) composes only when todayHub and dailyContinuity dependencies exist. `_todayActions` reuse resume/review/history/assessment/planning authority. `_selectLearningFromToday(fromTodayRoute:)` (1139+) verifies named current home/today then pops it; embedded Today must not accidentally pop the shell. Preserve old named secondary route behavior where retained and distinguish embedded callbacks.
- `_todayOwnerMatches` validates activeOwnerIdentities. Preserve all existing owner/pinned-content/session/review/recovery checks in the long callbacks. `TodayHubScreen` has a load-generation guard for replacement/dispose; it currently loads on init/useCases replacement, not tab/owner transition. Inspect how active-owner changes are exposed before embedding; prove stale/inactive callbacks cannot launch work or display a previous owner's snapshot. Avoid unnecessary background Today construction/load when hidden/uncomposed.
- `_NavigationEntry.isVisible` currently uses feature visibility; `ProductionFeatureGate` separately enforces registry state/durable composition and listens to kill switches. BuildFeatureRegistry excludes hidden/disabled/emergencyOff; inspect other registry implementations rather than assuming equivalent behavior. Do not use visibility alone to reveal disabled/uncomposed content. Today missing dependencies gets a truthful Thai empty/unavailable landing with existing Practice link only when permitted; never fabricated recommendations/history.
- ProfileSettingsScreen currently accepts only onOpenMastery and preserves owner subscription/active TickerMode behavior. Extend secondary access without weakening these lifecycle checks. Mastery/achievements currently rely on primary selection; move them to gated secondary routes and keep their child callbacks. Drawer secondary route IDs are already available. Profile links must remain available even when progress loading is empty/unavailable if the destination itself is permitted; inspect current loading branches.
- Native source is large (~2400-line navigation, ~3500-line navigation test). Read by symbols/ranges, not whole-file output. Existing tests assert five tabs, learning default, named Today secondary route, runtime removal, reduced configs, 360px/200% semantics and extensive owner/storage/replay behavior. Revise only intentionally obsolete navigation expectations; preserve/strengthen all real owner/storage assertions. Keep exact predecessor test bytes in backup before edits and record each fixture/expectation change.

## Implementation sequence and acceptance

1. Finish bounded route/feature/Today/profile inspection, save affected pre-edit bytes. Add meaningful widget/route RED for default four Thai tabs, Today-backed landing and Practice access without login/AI. Reproduce mismatch before production edit.
2. Implement one coherent shell with existing feature authorities. Preserve legacy explicit initial-entry semantics and stable IDs/back routes. Keep disabled/missing dependency paths truthful; do not populate four real features by bypassing gates. Reduced configurations may expose fewer real destinations; unavailable Today may be a non-feature shell message only.
3. Verify normally composed Today, missing Today dependencies, no registry, limited/disabled/emergency-off combinations; retained selection/fallback across visibility changes; profile access to mastery/achievements/quests/shop/history/settings and back returns to correct parent. Cover old initialIndex values and named internal route continuity. Ensure action bindings do not collide when an ID becomes a primary destination but an old secondary entry is retained.
4. Meaningfully test Thai label semantics and tap actions at 360px/200% text scale; no overflow. These host widget facts do not prove real screen-reader/device usability. Exercise owner drift during Today load/callback and tab switch/re-entry, retaining canonical safeguards. Existing TodayHost/owner tests must not be weakened.
5. Run bounded verification sequentially through `tool/cli/verify-scope.ps1 -Level Targeted -Area Runtime -TestTargets @('test/screens/main_navigation_screen_test.dart', 'test/navigation/navigation_glossary_test.dart')` (adjust exact affected target set after inspection). -TestName may narrow initial RED. Subsequent affected targets likely profile_owner_lifecycle_test.dart, today_hub_screen_test.dart, study_planning_hub_screen_test.dart, runtime_kill_switch_journey_test.dart; discover actual paths first. Review targeted AppBootstrap scenarios only if impacted; never run full 215/release/Flutter/backend/Android/GPU together. Inspect gate JSON/stdout/stderr BOM/hash, no source writes during gate. Reuse unchanged passed inputs.
6. Record source-pinned runtime host results separately from native/user pending; preserve unchanged prototype115+3 if its actual eight inputs remain exact. No release/sprint completion. Reassess useful independent work from actual source across all 12 groups; do not treat media/trial pending as a universal implementation stop.

## Exclusions and handoff

No new Phonics/story content, instructional scoring, database/schema/SRS/reward/AI admission changes, paid provider, deployment, research activation or browser restriction bypass. No subagents. Existing immutable Z snapshot and controller receipt retained. Current Astra/medium deterministic fallback, not Jev-selected; Standard/default requested and effective runtime tier unexposed. No Jev login probe/inference/ledger change.

Context boundary: X/Y runtime work and Z contract work are complete; AA is a new substantial native package with large composition and integration fixtures. Transfer exactly once with verified dirty/untracked/ignored snapshot before implementation. No AA RED or production code change exists to transfer. Successor performs RED then implementation, not merely acknowledges this card. Stop a failed method, discover/diagnose/correct and retest; never skip acceptance or use repeated failure as abandonment.

## Implemented host checkpoint — da20

S01-AA implementation and scoped host verification COMPLETE: 143 navigation/lifecycle/menu + 45 production-feature checks + 1 selected bootstrap scenario PASS (189 total). See evidence/S01-AA-validation.json for exact source pins, logs, preserved old bytes, intentional navigation fixture changes and confirmed RED. Normally composed entry is วันนี้ / ฝึก / คำศัพท์ / ฉัน. Explicit historical indexes and named Today route remain compatible; mastery/achievements and other existing utilities are secondary. Missing/disabled dependencies never fabricate real tabs or Today recommendations. Today load/action generations are invalidated on exit, replacement and durable owner changes.

This is not device, visual, accessibility-user, trial, sprint or release acceptance. UX-D01–25 remain OPEN. No new instructional content, scoring, schema, SRS, rewards or AI admission changes. Next independent package is S01-AB native Thai availability/recovery; no AB RED or implementation yet.
