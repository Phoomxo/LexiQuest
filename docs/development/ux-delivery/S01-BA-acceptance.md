# S01-BA — existing display preference recovery

Status: HOST_PASS371; 51 new tests, 33 reproduced RED resolved; external acceptance pending. See S01-BA.md and evidence/S01-BA-validation.json. Original scope follows. One existing SettingScreen theme/reduced-motion journey. Read AGENTS.md, README/state/latest handoff, S01.md, ready-backlog.md, readiness-12-groups.md and AZ validation/reassessment after exact-source import and sole-writer claim.

## Concrete scope

SettingScreen theme/reduced-motion native, semantics and optional menu callbacks retain _selectTheme/_setReducedMotion, which consult mutable _displayPreferences with only controller/busy admission and mounted-only failure messages. DisplayPreferencesController already serializes commands, pins expected owner and uses the canonical preference authority, but its mutation guard currently checks only controller disposal. Inspect actual queued read/save/owner/outbox boundaries before changing anything. Source hypotheses are not RED.

Reproduce meaningful widget/route/isolated-storage cases for retained controls after dependency/controller replacement or removal, canonical owner changes, initial/pending/error reads, duplicate explicit selections, route/tab/pop/disposal/lifecycle retirement and post-await notices. Preserve usable current controls, system/light/dark selection semantics, reduce-motion semantics, app-wide controller listener updates and existing no-op/serial ordering. A committed preference is not revoked. If acknowledgement is uncertain, use the existing canonical read authority and a separate explicit action; never automatically resubmit. Verify any changed mutation boundary with real isolated storage and owner checks. No second store, schema/wire protocol, new preferences, provider calls or costs.

Account/password/logout/local erasure/research-consent/participation controls are outside this package; preserve their source and behavior. No new content/media/difficulty/instruction/activity/scoring/session/event/learning writers, assignments/statistics/research/provider scope. Baseline offline without login/AI remains. Native/device/real restart/keyboard/screen-reader/visual/user/trial/release pending; UX-D01–25 OPEN and original S01 dates unchanged.

## Source and execution

Verify COMPLETE exact-base overlay, raw segments, Windows reserved names including superscript COM/LPT, containment, casefold uniqueness, exact ZIP membership/CRC/every size/hash/deletion. Copy manifest separately. Preserve ignored media/evidence and ALL original archives including XY/Z source-overlay.zip and every predecessor-changed-bytes.zip. Only new transport self-reference excluded. Predecessor immutable. Persist source receipt and sole-writer claim with ACTUAL CODEX_THREAD_ID; reconcile local LIVE ACTIVE writer/worktree/unused feature branch/currentItem/new revision and stale nextAction/checkpoint/continuation/dispatch/nextDispatch before messaging claim or application edits.

One writer/package, no subagents/background implementers. Sequential bounded tool/cli/verify-scope.ps1 using exact discovered targets; full release only frozen SHA. Preserve result/streams immediately; compare canonical path/hash maps across Learning/Runtime. No source edits/concurrent heavy checks during gates; no passed unchanged reruns. Keep original RED/final source snapshots and all inherited originals. Reassess all twelve groups and continue useful ready plan-v5 work automatically; justified context boundary requires complete immutable handoff, writer release and exactly one saved-project successor with reused ID.

Mandatory recovery interpretation and CURRENT IMPLEMENTATION AUTHORIZATION: plan-v5 implementation and sequential saved-project succession are authorized; older analysis-only limits superseded. Stop failed METHOD, discover real paths/symbols, diagnose, correct and retest automatically. Repeated failures or ten minutes without progress require a NEW diagnostic approach, NEVER abandonment/hidden RED/weakened assertions/false PASS/writer release to bypass gates. Stop only explicit user pause or concrete indispensable external prerequisite with no useful independent authorized work. Preserve exact unresolved checkpoints.

Astra/medium deterministic fallback, NOT Jev-selected. Standard/default requested/runtime tier unexposed. Original Jev USD5 ledger/quota/cache/login block/heartbeat unchanged. Fresh executor/free-credit/auto-reload/shared-spend/quota evidence and durable reservation before live routing. No reset/purchase/pending clearance/repeat login probes, Security/Codex Security/Security Scan/Deep Scan, deployment/paid provider or browser rejection bypass. Thai updates; no repeat approval for authorized work.

UTF-8 explicit; verifier logs can be UTF-16 BOM/mojibake Thai. TestName is plain-name, not regex. Initial package setup can cause environment-only InputDrift; preserve/diagnose. Never await void handleAppLifecycleStateChanged. Observe synchronous/immediate Future errors. Context-dependent initialization belongs after dependencies exist. Real SQL widget reads need bounded real IO and fake frames; release held test Futures before assertions and dispose subscriptions before database closure in the widget zone. Existing post-commit readback fixtures must delay only after mutationCalls > 0; retain original row/outbox/restart/session assertions. No shared-user-data tests.
