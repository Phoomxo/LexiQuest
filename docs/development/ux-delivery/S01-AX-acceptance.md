# S01-AX — existing explicit personal word-delete recovery

Status: HOST_PASS at the host layer only; 535 scoped tests, 32 new cases, 27 meaningful original-source RED resolved. No remaining AX RED. Native/device/user/trial/release pending. See S01-AX.md and evidence/S01-AX-validation.json.

Read AGENTS.md, README/state/latest handoff, S01.md, ready-backlog.md, readiness-12-groups.md and AW validation/reassessment. Verify COMPLETE exact-base overlay including raw path segments/Windows reserved names (superscript COM/LPT too), containment, casefold uniqueness, exact ZIP membership/CRC/every size/hash/deletion. Copy manifest separately. Preserve ignored media/evidence and ALL original archives, including inherited XY/Z checkpoint source-overlay.zip and every predecessor-changed-bytes.zip. Predecessor immutable. Persist source receipt/sole-writer claim with actual CODEX_THREAD_ID and reconcile local LIVE ACTIVE state writer/worktree/branch/currentItem/new revision and stale nextAction/checkpoint/continuation/dispatch/nextDispatch BEFORE messaging claim or application edits.

One coherent EXISTING explicit personal word-delete journey. VocabListScreen _deleteWord retains mounted/feature-only entry, raw owned-dialog cancel/confirm callbacks, ordinary post-await message; _mcpDeleteWord has existing optional owner/registry/current-route verification and committed readback. Confirm meaningful widget/route/isolated-Drift RED for duplicate dialog/action, owned-dialog usability, pending read/retry/uncertain acknowledgement, retained cancel/confirm/optional controls, owner/dependency/category/word replacement/removal and route/tab/pop/disposal/lifecycle plus any in-flight canonical mutation boundary BEFORE fixes. Preserve ordinary and optional menu paths, Thai recovery, original category/query return, canonical owner/category/word/outbox authority, explicit-only writes and truthful acknowledgement/read-only committed reconciliation. Already committed deletes are not revoked. Keep category/create/edit/import algorithms separate. No parallel writer or new mutation authority.

No new content/media/difficulty/instruction/activity, completion/scoring/session/event/learning writers, assignment/scheduling/statistical reporting/research/schema/wire protocol/provider work. Isolated DB/provider/voice doubles only; no actual user-data import/call/upload. Baseline offline without login/AI; qualified content/media/trial gates remain. Native/device/real restart/real keyboard/screen-reader/visual/user/trial/release pending; UX-D01–25 OPEN and original S01 dates unchanged.

Sequential bounded tool/cli/verify-scope.ps1 exact discovered targets; full release only frozen SHA. No concurrent heavy checks/source edits during gates or rerun of passed unchanged closure. Preserve each completed result envelope and streams immediately; same selection overwrites. Compare canonical path/hash maps rather than area-label dictionaries. Preserve RED/final source checkpoints and all inherited originals. Reassess all twelve groups and continue useful ready plan-v5 work automatically.

Recovery: stop failed METHOD, discover/diagnose/correct/retest automatically. Repeated failures or ten minutes without progress require NEW diagnostic approach, never abandonment/hidden RED/weakened assertions/writer release to bypass gates. Stop only explicit pause or indispensable external prerequisite with no useful independent work. At justified context boundary preserve COMPLETE immutable source/evidence, release writer then dispatch exactly one saved-project successor and reuse its ID.

Astra/medium deterministic fallback, not Jev-selected; Standard/default requested/runtime tier unexposed. Original Jev USD5 ledger/cache/quota unchanged. Fresh executor/free-credit/auto-reload/shared-spend/quota evidence and durable reservation before live routing; no resets/purchases/pending clearance or repeated controller login probes. Heartbeat unchanged. No Security/Codex Security/Security Scan/Deep Scan, paid provider, deployment or browser rejection bypass. One writer/package, no subagents/background workers. Thai updates; no repeat approval.

Diagnostics: UTF-8 stdout/files explicit; verifier streams can have UTF-16 BOM and mojibake Thai. TestName is --plain-name, not regex. Discover paths with rg --files, no literal Windows wildcard arguments. First Flutter setup can change environment fingerprint without changing source; preserve InputDrift and obtain stable result. Owned native Save may be tapped before owner read resolves: queue only that explicit request, await initial read and revalidate; never silently discard it or auto-resubmit. Optional committed reconciliation is READ-ONLY and may remain usable after cover/return while old writes stay retired. A canonical owner rejection is stale context, not invalid category/word text. Real SQL fixtures need bounded real IO and fake-frame pumping, plus stream-close pumping after disposal; create DB/owner objects and await initial setup inside the widget zone, not serialized initialization inside runAsync. AW two combined diagnostic stalls resolved with isolated/instrumented verification and unchanged original fixture; final groups stable. Inspect exact formatted lines/apply_patch and formatter LASTEXITCODE; do not repeat failed string replacement.
