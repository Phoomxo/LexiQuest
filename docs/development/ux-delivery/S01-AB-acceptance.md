# S01-AB — Thai native availability and saved-session recovery

Status: SOURCE FINDINGS ONLY; RED and implementation NOT_STARTED. One coherent recovery journey package, following completed S01-AA native shell. Authority: approved v5 §§15.3 R2-03 / 15.5, continuous sequential implementation. No new instructional unit.

## Outcome and concrete source

Users encountering a disabled/uncomposed/stale destination or an unavailable saved mission receive understandable Thai copy and an accessible safe way back or to retain work. Existing source still shows English `Feature unavailable` and an English body in `lib/runtime/production_feature_gate.dart`; `MainNavigationScreen._resumeFromToday` shows `Mission cannot be reopened`, `Keep saved`, and `End mission`. These are visible existing-interface discrepancies, not speculative malformed inputs. Read source-pinned twelve-group reassessment `evidence/S01-AA-backlog-reassessment.json`.

## Acceptance and sequence

1. Read incoming source/receipt/state and verify all exact bytes before claim. AA is HOST_PASS only: 143 navigation/lifecycle/menu + 45 production gates + 1 selected bootstrap, all final source pins verified. Prototype Y 115+3 reused on exact eight pins. UX-D01–25 and native/user/trial acceptance remain open; S01 dates unchanged.
2. Inspect shared unavailable widget, typed feature reasons, pushed versus root route behavior, Today/Profile retry/fallback and existing saved-dialogue recovery. Preserve stable route IDs, registry visibility/enabled/limited/emergency-off decisions, durable composition, current owner/load generation and every storage/pinned-content safeguard. Do not reveal disabled features or mutate flags to provide recovery.
3. Save pre-edit source/test bytes. Reproduce meaningful widget/route RED for Thai unavailable copy and real recovery interaction before production edits. Cover root and pushed gates, back returns to correct parent without popping root, 360px/200% semantics and tap, and live state changes. Shared UI is the unit; do not create per-handler tasks.
4. Implement a consistent Thai-first recovery surface for these existing unavailable/saved-work paths. Use action wording that conveys what is kept and what ends. Offer retry only through an existing supported authority, never imply refreshing reenables a kill switch or repairs corrupted storage. Preserve explicit abandon semantics and owner revalidation; default/back/cancel must retain saved work. Do not expose implementation details or raw errors to learners.
5. Run sequential affected `tool/cli/verify-scope.ps1` gates. Discover exact paths; likely `test/runtime/production_feature_gate_test.dart`, `test/scenarios/production_feature_navigation_test.dart`, `test/scenarios/runtime_kill_switch_journey_test.dart`, `test/screens/main_navigation_screen_test.dart` and a meaningful selected saved-dialogue scenario. Inspect BOM-aware JSON/logs and hashes; no source writes during gates; no full release unless frozen SHA. Reuse passed gates only for unchanged actual pins. No full Flutter/backend/Android/GPU concurrency.
6. Self-review under no-subagent instruction; record exact source preservation and host versus native/user pending. Reassess useful independent backlog across all 12 groups after completion; no blanket media/trial stop, empty waiting or artificial per-handler successor.

## Exclusions and continuation

No new Phonics/story/exam units, media acquisition/approval, scoring/schema/SRS/reward/AI admission changes, paid inference, purchase, deploy, research activation or external participants. Respect prior browser rejection; no alternate browser/CDP/headless/localhost bypass. Current GPT-6 Astra/medium deterministic fallback, not Jev-selected; Standard/default requested, runtime tier unexposed. Jev original USD5 ledger/cache unchanged; no live call without fresh free/pricing/shared-spend/quota and reservation, no reset/pending-clear/top-up/paid fallback or repeat login probes absent changed signal. Antigravity requires fresh CLI/quota/writer isolation; unavailable means continue current fallback.

At a genuinely justified context boundary, preserve an immutable complete snapshot, hashes, evidence, OPEN/RED, next useful scope and recovery, release writer, then list_projects and dispatch exactly one successor in saved project e0d28f74-0d26-40bc-b126-4cfebe4ff7a0. Reuse existing/pending successor ID. No unresolved RED transfer to bypass a gate. Stop a failed method, discover/diagnose/repair/retest; repeated failures never authorize abandonment or writer release. Stop only explicit pause or indispensable external prerequisite with no useful authorized independent work. Do not wait for the existing heartbeat or ask for already-granted permission.

## Result

HOST_PASS: 128 scoped tests, nine new recovery cases; evidence/S01-AB-validation.json. No remaining RED. Native/device/real accessibility/user/learning/release acceptance remains pending.
