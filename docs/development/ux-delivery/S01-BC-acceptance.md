# S01-BC — existing explicit logout recovery

Status: HOST_PASS at the offline host layer; native/user/release pending. Latest user instruction stops succession after this checkpoint. See evidence/S01-BC-validation.json.

Original acceptance scope: One existing SettingScreen logout journey, after BB host completion. Read AGENTS.md, UX README/state/latest handoff, S01.md, ready-backlog.md, readiness-12-groups.md, BB validation/reassessment and this card after complete exact-base source import and sole-writer claim.

SettingScreen _logout retains direct native/semantics callbacks, reads mutable _account, guards account/busy only, awaits signOutToLocalGuest then resets navigation using mounted only, and catches AccountException only. AccountUseCases.signOutToLocalGuest reads/clears entry state, creates a local guest, invokes gateway signOut, and owns rollback/entry restoration/transition notification. These are source hypotheses, not confirmed RED. Inspect the actual owner/session/gateway/entry boundaries and existing tests before changes.

Reproduce meaningful offline tests for retained entry/duplicate submission, replacement/removal of account/dependencies/session, tab/route/immediate pop/disposal/lifecycle retirement, synchronous/untyped/reauth-style provider errors, entry/owner failures and late acknowledgements. Preserve usable current controls, explicit-only logout, canonical owner upgrade/rollback/entry restoration and baseline local use. No automatic logout, retry, reset, sign-in, email or account operation. Use isolated fakes and private in-memory stores only; no real Firebase/auth services, credentials, shared-user-data tests, emulator setup, cost or network account actions.

Committed provider sign-out and committed local transitions cannot be assumed reversible. Distinguish failure before mutation, partial/uncertain acknowledgement, and committed outcome. Do not invent readback or success, revert a newer owner, retry automatically, erase data or introduce authentication protocols/providers/stores/schemas. Add optional in-memory admission only where meaningful RED proves need; preserve existing canonical rollback/restoration policy rather than blindly aborting midway through it. Any recovery must require a separate explicit user action through existing authority.

Keep BB password dialog/gateway recovery, BA display controls, local erasure, consent/participation, learning/import/content/scoring/session/event writers outside this package and preserve behavior. Baseline works without login/AI. No instructional/content/media/provider/research expansion. Native/device/real restart/keyboard/screen-reader/visual/user/trial/release stay pending, UX-D01–25 OPEN; original S01 dates unchanged.

Verify COMPLETE exact-base overlay: raw segments, Windows reserved names including superscript COM/LPT, containment, casefold uniqueness, exact ZIP membership/CRC/every size/hash/deletion. Copy manifest separately. Preserve ignored media/evidence and ALL inherited original archives, especially XY/Z checkpoint source-overlay.zip and predecessor-changed-byte/doc archives. Only new transport self-reference is excluded. Predecessor is immutable. Use an isolated checkout; inspect actual branch, create an UNUSED feature/ branch at exact base before import. Never reset/clean/write predecessor or user checkout. Persist source receipt and sole-writer claim with ACTUAL CODEX_THREAD_ID. Reconcile local LIVE ACTIVE writer/worktree/branch/currentItem/new revision and stale nextAction/checkpoint/continuation/dispatch/nextDispatch BEFORE messaging claim or application edits.

One saved-project writer/package, no subagents/background implementers. Sequential bounded tool/cli/verify-scope.ps1 gates with exact discovered targets. Full release only frozen SHA. Preserve result/streams immediately; compare canonical path/hash maps across Learning/Runtime. No source edits or concurrent heavy checks during gates; no passed unchanged reruns. Keep original RED/final source checkpoints and inherited originals. Reassess all twelve groups and continue ready plan-v5 automatically. A justified context boundary requires complete immutable handoff, writer release and exactly one saved-project successor; reuse its ID.

Mandatory recovery interpretation and CURRENT IMPLEMENTATION AUTHORIZATION: plan-v5 implementation and sequential saved-project succession are authorized; older analysis-only limits superseded. Stop failed METHOD, discover real paths/symbols, diagnose, correct and retest automatically. Repeated failures or ten minutes without progress require a NEW diagnostic approach, NEVER abandonment, hidden RED, weakened assertions, false PASS or writer release to bypass gates. Stop only explicit user pause or concrete indispensable external prerequisite with no useful authorized work remaining. Preserve exact unresolved checkpoints.

Astra/medium deterministic fallback, NOT Jev-selected. Standard/default requested/runtime tier unexposed. Original Jev USD5 ledger/quota/cache/login block/heartbeat unchanged. Fresh executor/free-credit/auto-reload/shared-spend/quota evidence and durable reservation before live routing. No reset/purchase/pending clearance/repeat login probes, Security/Codex Security/Security Scan/Deep Scan, deployment/paid provider or browser rejection bypass. Thai updates; no repeat approval for authorized work.

BB diagnostics: FirebaseAuth.currentUser creates fresh wrappers; do not compare User object identity. BB uses auth-state snapshot/events plus UID and caller admission, awaits initial snapshot before reauthentication, rechecks before update, and never rolls back a sent update. Same-UID events retire controls. No password material in receipts/logs; fixtures synthetic. In-memory AccountSession.sessionIdentity is not persistence. Initial original RED includes environment InputDrift/controller cascades; don't claim its raw fail count as confirmed defects. Corrected lifecycle fixture uses inactive, never await void handler. Stream ordering and SDK wrapper regressions are resolved. Effective account rebinding is shared to keep visible identity aligned. Inherited logout/local-erasure/consent method bodies remain unchanged.

UTF-8 explicit; verifier streams may contain UTF-16 BOM/mojibake Thai. TestName is plain-name, not regex. Initial environment setup can produce InputDrift: preserve/diagnose. Observe synchronous/immediate Future errors. Initialize after dependencies exist. Wait bounded real IO for SQL fixtures, release held Futures before assertions and await actual menu/gateway futures before disposal. Dispose subscriptions before database closure. No shared-user-data tests.
