# Autonomous verification after native UI fixes — 2026-09-08

Status: THIS AUTONOMOUS BATCH COMPLETED WITH OPEN PRODUCT FAILURES AND UNCOVERED ACCEPTANCE. User authorizes tests that can run without their cooperation; human hearing/speaking/comprehension/TalkBack and learner UAT remain deferred, not passed. A separate automated real-clock 180-minute endurance run is also not yet executed; it must not be labelled human-dependent or passed by these shorter tests. No model training, production rollout, research enrollment/upload or user-data erasure occurred in this test run.

Worktree: `C:/Users/Phet/.codex/worktrees/02fa/LexiQuest`; branch `codex/pair-matching-pm0-pm8`; HEAD `788e90e62b1694c20945734787723c168b6a6ab2`. Existing dirty work is preserved. Product source baseline is 1,498 inputs, fingerprint `3536599289dbf74d8888d80f8639457eedaf6f16161db69052696e424e16e513`. Previous 5,029-test regression is existing evidence; it is not represented as rerun here.

## Work ledger

1. Diagnose native launcher/observability: current instrumented launcher starts and opens MyApp through an observed physical tap; earlier failure cause remains undetermined.
2. Execute current-source real model/camera inventory/local TTS suite: six substantive native cases plus final sentinel PASSED; restore independently reconciled after lost USB acknowledgement.
3. Build and run synthetic navigation at the actual Android viewport: `autonomous-ui-supported-reminder` PASSED all 16 cases with 56 valid images / 28 distinct pages. Every case cleanup and the final sentinel passed; restoration succeeded with no pending reconciliation.
4. Current-source performance benchmark failed its frame-time budget. Actual visual review confirmed missing stage-2 reading content and interface copy issues; product fixes remain open. Native export cancellation and two-process persistence checks both passed their functional oracles.
5. Final manual QA APK `1e859...` installation and launch were verified at 12:49:24 UTC after one reconciled recovery from the last USB restore-push failure. Product source and all three final fixture/APK identities were verified unchanged. No root test/build/install process or unresolved restoration remains.

## Observations already obtained

- Read-only initial check verified installed manual QA APK `89a07a60ad8c434bdab0d490e61d0258bdf1cbfdd5829b30d08b518dcd6beaf1`; no app PID initially. Authorized cold launch returned success.
- Full `dumpsys window` contains two current-focus entries for this package on this vivo, causing the old helper's exactly-one-entry check to reject it. Scoped `dumpsys window displays` reports the physical display 0 and one current app focus. This is an observability issue, not evidence of a product navigation defect.
- `autonomous-diagnosis-20260908T110039Z` captured the visible manual launcher and raw UIAutomator output. UIAutomator exit 0 returned no hierarchy; stderr contains vivo CPU-info EACCES and `ERROR: null root node returned by UiTestAutomationBridge.` Do not infer a causal relation between those two errors, or infer app freeze from this failed tool output.
- The screenshot still shows the earlier voice-playing launcher state despite a cold launch/new PID; startup/navigation stage instrumentation was added only to the isolated manual fixture to discriminate stale surface/startup/callback causes. No product behavior changed.
- Current native fixture build passed: `logs/fixture-native-20260908T105929511122Z/summary.json`; APK `motivation-autonomous-native-debug.apk` SHA256 `e369ff406d69be302ba510115651abb2dee28caddad30a74fd7429fa567ad330`, 223,955,799 bytes. Product source stable.
- First native execution stopped on install exit 255 after successful push/USB interruption: `autonomous-native-latest/journal.json`. The immediate oracle still saw old manual bytes. Later read-only reconciliation found exact candidate `e369ff...` installed and no active `pm`/`cmd package install` command. Do not overwrite the original uncertain-install record or reinstall the same APK. The wrapper restored its temporary Flutter log-tag setting.

## Notification and manual launcher observations

`autonomous-services-first/journal.json`: production notification initialization/permission query passed. Schedule/cancel and delivery were explicitly deferred because existing permission was unknown; no permission prompt or notification was created. All executed-case oracles and cleanup checks passed, but the suite outcome is `deferred`, not an all-services PASS. APK restored and source stable.

Instrumented manual APK `1e8590380f9306f2a43fa930ffa4cb5f350138c6ef761e920053eb413b3eed13` installed with acknowledged exit 0 and no pending reconciliation. `autonomous-manual-instrumented-install/startup.log` proves binding/storage/bootstrap/seeding/runApp/first-post-frame stages. The Flutter log tag was restored. A screenshot-grounded tap on the open-app button reached the real ChooseModeScreen, captured in `device/manual-ui/autonomous-instrumented-after-transition.png`. The immediately preceding capture occurred before the transition; no repeated tap was needed. This establishes current launcher navigation, not the cause of the old launcher symptom.

## UI first run and correction

`autonomous-ui-first/` preserves driver failures, structured results and seven screenshots; restoration passed. Only `tabs-progress-profile` passed; remaining cases failed with `Call convertFlutterSurfaceToImage() before taking a screenshot`. Installed integration_test SDK reverts the image surface after each test, so the fixture now reconverts per test. Independent fixture review corrected Quiz's next-button finder, adaptive time picker confirmation, Shadowing unavailable wording, and added explicit Scramble reset, Associative stage-2 and Cloze exit checks. Product code was unchanged.

The extended SDK driver prints `All tests passed.` to host stdout when its framework response passes. UI verification now uses that actual primary result plus exact 16 framework results, ordered structured START/PASS events, final sentinel, owned cleanup and all PNG/physical-viewport receipts. Empty native logs are explicitly unavailable, not asserted error-free. Observed native errors still fail the run. Runner syntax and 26 bounded synthetic oracle checks passed. Corrected UI fixture analyzer exited 0 with no errors/warnings and four style infos. Corrected APK `0812273be106810b280acf9ba8bf9ab940d5cba89d6aefe79268e76b5216ad26` built on unchanged product source; build success is not UI test success.

## Confirmed UI findings and remaining acceptance gaps

- Associative Reading stage 2 shows the instruction to reread but omits the passage. Confirmed in actual `associative-stage-two.png` and `lib/screens/associative_reading_session_screen.dart` case 2. The navigation assertion passing does not certify this activity's content completeness. Product fix remains open.
- Rejected/unapproved Cloze content displays `Skipped. The cloze example has not been approved.` directly from its semantic announcement. This is a confirmed English system-message leak, separate from correctly rejecting unapproved content. The analogous Definition path is a code-only finding, not a captured native case.
- Actual shop screenshots expose “Material 3” in a learner-facing theme description, and the empty calendar explanation contains “active”. These are copy issues; synthetic English vocabulary/category names are not counted as untranslated interface text.
- Disabled hint text looked faint in screenshots. This is an observed usability concern; no measured contrast-ratio failure has been established.

The 14/16 seeded run (`autonomous-ui-seeded`) captured 50 valid screenshots / 26 distinct pages. Settings, Today, planning and SRS now passed with truthful guest/opt-in/canonical seed conditions. Two fixture failures remained: time-picker finder selected the underlying goal dialog, and the synthetic lexical loader omitted production packaged-voice bytes. Final fixture now narrows the real picker and falls back only to known verified packaged voice identities. No production verifier/feature flag was weakened. The original seeded-run restore failed during USB push; `autonomous-ui-seeded/restore-push-reconciliation.json` established unchanged candidate/no active installer and `autonomous-ui-seeded-restore-recovered/install.json` records one acknowledged restoration to manual `1e859...` with no pending reconciliation.

Actual Android export cancellation and two-process persistence checks were added as bounded synthetic fixtures to cover independently executable gaps. Their native results are recorded below; creating/building them alone is not acceptance evidence. No human permission prompt, actual saved export, OS network toggle, real-participant enrollment or 180-minute journey is claimed.

`autonomous-ui-final` subsequently passed 15/16 cases, producing 54 images / 27 pages; restoration passed. Cloze now passes with verified synthetic content and known packaged voice assets. The remaining goal reminder failure occurs because the fixture bootstrap defaults to `UnsupportedReminderScheduler`; the production UI correctly hides its unavailable settings. A supported synthetic scheduler will exercise that picker without requesting Android permission or scheduling a notification.

The first `autonomous-export-cancel` run failed before opening DocumentsUI: Dart resolves the approved Android private path to `/data/data/...`, while the fixture expected the equivalent `/data/user/0/...` string. `post-failure-fixture-log.log` contains the exact assertion. The correction compares both paths through the same canonical-resolution API, preserving the exact package and owned-directory guard. Driver startup was also moved before handshake polling to retain early failure output; startup ordering is not the established cause of this failure. Restoration initially failed during USB push; read-only reconciliation established unchanged fixture bytes and no active installer, then `autonomous-export-cancel-restore-recovered/install.json` records one successful restoration to manual `1e859...`. No picker Back, file save or user-file inspection occurred in this failed run. The new persistence fixture has equivalent path guards corrected before its first execution.

## Final UI navigation result: PASS within the selected scope

`autonomous-ui-supported-reminder/journal.json` and `response.json` prove 16/16 selected cases, 56 PNGs and 28 distinct production page types on the actual 1080×2292 Android view, DPR 2.75, text scale 1.0. APK `20d57414cca88f7052e304bf955374cbbc06479e8362dc7f572edf1e34d9c487` was built with frozen target `740c8878...`, driver `fa2f289...`, external fake `eced812...` and unchanged product source. SDK driver exit 0, all case/cleanup/sentinel oracles passed, and manual `1e859...` was restored. The reminder case alone uses a supported synthetic scheduler; receipts prove zero permission requests, schedules and cancellations. Root inspected both new reminder screenshots and found no clipping; the separate latest visual review inspected seven Today/goals/SRS/Cloze images without a new layout defect.

This result covers the enumerated actions, not every button/state in the app. Native Cloze submission, Scramble completion, SRS grading/finish, on-screen keyboard variants, OS text-scale/theme/reduced-motion changes, and destinations outside the selected inventory were not all exercised here. Existing matched-source host tests cover additional cases. The confirmed stage-2 passage omission and copy findings remain open despite navigation PASS.

The corrected export APK `aaf6e320...` reached `ready-to-open` with app foreground verified, then `autonomous-export-cancel-canonical` failed because the adapter called an inherited creation helper whose static allowlist excludes its new handshake file. No picker release or Back occurred. Restoration passed with no pending ownership; the runner-only exact-path creation fix and subsequent successful functional retry are recorded next. This historical failure is a fixture failure, not a product cancellation failure.

## Real Android export cancellation: functional PASS, restoration recovered

The runner-only correction preserved the existing generic/native helper guards and added exact-run, absent-first, no-clobber creation for its one approved handshake path, with read-only hash reconciliation after a lost acknowledgement. Eight bounded tests through the actual creation-method chain passed, including rejection/uncertain-write/restore-blocking cases. No Dart/product/APK rebuild was needed.

`autonomous-export-cancel-owned-write` on the same APK `aaf6e320dc64dd9554f4f347c42e21ac287ca51a17351ce182f571b4b7a21bed` observed the resolved Android DocumentsUI picker, verified the same-run handshake and app foreground, sent exactly one Back, observed a typed `ExportFailureCode.cancelled` from the production store, and verified return to the app. The cancellation token was untouched and no saved destination returned. Driver/framework/sentinel/native oracles passed. The runner neither selected a destination nor browsed/screenshot user files; this is cancellation evidence, not actual saved-file/readback acceptance. All four owned markers were removed; final restoration status is recorded in the closure below.

## Native committed-data persistence: PASS

`autonomous-persistence-canonical/journal.json` and both `phase-a/response.json` and `phase-b/response.json` passed on APK `2a576df257c315bd6483e6469b6af63028d5fd22c6076d15eece9598420abb64`. Phase A used production local learning APIs to commit a synthetic category/word, one correct typed-recall attempt, one completed session, its SRS row, progress and reward state to an isolated SQLite file. The real connection stayed open until an identity-verified host force-stop. Phase B launched the same APK as a different Android process and reopened the exact same database without reseeding; the durable witness, IDs and all canonical rows/progress/rewards matched. Research rows remained zero. Both phases returned driver exit 0 and their exact case/sentinel results; scoped native logs were available with no observed errors.

This verifies persistence of completed writes across process death, not interruption mid-transaction, power loss or recovery of an unfinished live-user session. The owned test directory was inventoried and removed under exact ownership/hash guards. Restoration to manual `1e859...` passed, no pending reconciliation remains, and product source stayed unchanged.

## Current Adventure profile: FAIL

`autonomous-profile-current/response.json` is a fresh physical Android AOT/profile run on product fingerprint `35365992...`, APK `02d4c23f2e68e751b67e7d954837bd4851991f4a399c85d34683c6db1fdfc061`. The existing benchmark ran 20 transitions / 120 measured frames. Map/list frame p95 was **22.574 ms**, exceeding the unchanged **16.7 ms** budget; maximum frame was 23.192 ms. Driver/test correctly failed. Entry p95 0.013 ms, journey projection 0.377 ms, first meaningful render 70.295 ms and paired session-start overhead 0.308 ms met their budgets. No frame or synchronous task exceeded 100 ms; authority and learner-pause invariants passed. This is a real remaining performance acceptance failure, not converted to PASS by unrelated checks.

Restoration to manual `1e859...` completed, source/pins remained stable and no pending reconciliation remains. Read-only after-run thermal evidence reports Android thermal status 0; this does not prove the device was never throttled during the run or establish the cause. Earlier September 7 profile PASS belongs to an older source snapshot and does not override this current result.

## Current native run details

`autonomous-native-resumed/native-pid-flutter.log` and `response.json` prove three real LiteRT cases (inference, wrong-output-contract rejection, CPU/XNNPACK benchmark), camera inventory (front/back), and EN/TH local-only TTS completion. All native oracles are true, driver exit 0, no native error events. CPU median 56,078 microseconds; XNNPACK median 44,918 microseconds, ten samples each. These tiny model benchmarks are not whole-app performance acceptance or recognition accuracy evidence.

The runner's final exit remained 1 because restore returned 255; the historical journal truthfully retains that transport failure. Subsequent `autonomous-native-resumed/restore-reconciliation.json` at 11:11:37 UTC verifies original manual APK `89a07...`, no active installer and original empty Flutter log tag. No duplicate installation was issued. Native tests PASS, restoration observed complete later; do not report the original orchestration exit as 0.

The operational page inventory is in `build/verification/motivation-ui-20260908/reports/autonomous-page-coverage-plan.md`. Its 28-page slice is newly enumerated from real code, not an invented historical list; additional enabled destinations are listed separately. Older 28/99 widget captures are not 28/99 distinct physical pages. Synthetic viewport/navigation checks cannot certify perceived audio quality, actual learner motivation, research efficacy, live cloud service or a physical exported file when a fake export store is used.

## Acceptance still outside this autonomous batch

The real-clock 180-minute endurance run has no reviewed executable harness in the inspected repository and was not run. It remains a separate autonomous engineering task, as documented in `reports/autonomous-native-test-plan.md`; it is not cancelled by the user's instruction to skip human cooperation. Other native gaps are avatar memory/frame profiling, OS network interruption, actual export save/readback, and the complete OS theme/200% text/reduced-motion matrix. The completed host regression and selected physical-device cases do not replace these checks. The current frame-time failure and missing reading content also remain product work before broad acceptance can be claimed.

## Final restoration and evidence closure — 12:49:24 UTC

The last export suite's functional checks passed, but its original wrapper exited 1 after a restore-push transport error (`20-byte write failed`). Three read-only observations established the same export fixture APK and no active installer; the failed journal remains unchanged. One subsequent recovery in `autonomous-export-owned-write-restore-recovered/install.json` returned exit 0, verified manual APK SHA256 `1e8590380f9306f2a43fa930ffa4cb5f350138c6ef761e920053eb413b3eed13`, reported successful launch, and left no pending reconciliation. This closes restoration separately; it does not rewrite the original wrapper as PASS.

The final post-restoration source manifest is `autonomous-export-owned-write-restore-recovered/whole-source-after.json`: HEAD `788e90e62b1694c20945734787723c168b6a6ab2`, 1,498 inputs, fingerprint `3536599289dbf74d8888d80f8639457eedaf6f16161db69052696e424e16e513`. All final UI/export/persistence archive hashes match their identity files and every pinned fixture input matches its build record. Focused Dart analysis of the three changed ignored targets found zero errors/warnings (4/3/4 style infos); Python adapters passed syntax/review checks. Product files were not edited in this testing continuation, and pre-existing dirty changes were preserved. No test/build/install process remains active.
