# E5.1 — Dialogue missions

Accepted local implementation on `lexiquest/post-g83-e51`, based on `962542ea09bce9a651c5ca769d788fd0761d9438`. The exact accepted commit is pinned in external `POST-G83-E51-accepted-handoff.json`. Production rollout defaults off; device, assistive, human calibration and release gates remain E6/E7.

The learner can enter an authored dialogue from Adventure, make a contextual language choice, read its saved consequence, complete a bounded repair, and return to the existing canonical result. Save/exit is available throughout. Unavailable saved content offers keep-saved or canonical abandonment; it never silently substitutes a revision.

The three original scenes cover book, pencil and bottle. Titles/objectives do not disclose the answer before the initial recognition response. Every branch has a language objective and consequence, and every admitted path terminates in at most two choices. For example, choosing the wrong object at the library preserves the incorrect recognition attempt, presents an explanation, and records the next response as assisted practice. Scene progress does not claim independent recall.

`CanonicalAdventureSessionComposer` still admits the content. One owner-lease SQLite transaction commits canonical evidence plus an immutable branch receipt in the existing Learning checkpoint stream. Exact scenario, lexical artifact and core-content hashes are distinct pins. Replays return accepted receipts; collisions and stale backtracking fail. Capability retirement fences owner, feature, route and background changes, including changes during admission. Schema 34, 58 named tables, 48 owner descriptors and historical checkpoint decoders remain unchanged.

Validation: **460 tests across 19 targeted files passed**, with all 1437 verifier input hashes and command log hashes revalidated. Static analysis: zero errors/warnings, seven inherited informational lints. Coverage includes every authored terminal path, physical SQLite restart after lost acknowledgement, atomic rollback, owner upgrade/export/delete, future/incomplete recovery rejection, Adventure entry/result navigation, 320px/text200 UI, runtime retirement, historical recovery and migration checks. The inherited all-owner-table fixture now seeds four previously omitted extension tables and checks exact identity/payload retention; no assertion was weakened.

Detailed pins, test receipts and fixes are in `post-g83-e51/acceptance.json`, `content-review.json` and `defects-and-review.json`; raw red/green logs are external under `evidence/E5.1/all-runs`. Direct review followed the no-subagent policy. No remaining required implementation defect is transferred. Next authorized slice: E5.2/F09, only after accepted commit, durable handoff and writer release. Standard/default requested; effective service tier remains unverified.
