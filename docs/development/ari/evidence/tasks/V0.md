# อารี V0 — ตรวจช่องทางบัญชีและมือถือ

ตรวจเอกสาร 2026-09-20 · input `c3617a2dcaa1e57eedaf2de750eb7dd9c8d25528` · branch `feature/ari-feasibility`

**ตรวจครบ 3 งาน; เส้นทางครบเงื่อนไขยัง UNCONFIRMED ส่งต่อ V1 ไม่เปิด V2 และไม่ปิด E7** ผลนี้เป็นเอกสาร ไม่ใช่ runtime PASS หรือข้อสรุปว่าไม่มีทางทำได้

| งาน | ผล |
|---|---|
| V0.1 — REVIEWED | แยก identity ของ Sites/partner, OAuth grant ให้ MCP เข้าทรัพยากร และ managed ChatGPT auth ของ Codex [S1–S3,S5,S6] |
| V0.2 — REVIEWED | ChatGPT-auth Codex มีสิทธิ์ตามบัญชี; API-key ใช้ billing แยก ไม่พบหลักฐานว่า MCP ย้าย quota ให้ Flutter [S5–S7] |
| V0.3 — UNCONFIRMED | Free รวมอยู่ใน Codex ตามเอกสาร แต่ Free ไทย + native LexiQuest + ไม่มี companion ยังไม่ยืนยัน [S5,S7,S11] |

| Route | Fact ตามเอกสาร | Inference / unknown |
|---|---|---|
| Identity login | ยืนยันตัวตนและ tool grant แยกกัน [S2,S3; ใช้หลักฐานเดิม] | login อย่างเดียวไม่พิสูจน์ inference |
| MCP | client เรียกทรัพยากรแอป [S1; เดิม] | UX ที่สนทนาใน ChatGPT ไม่ตรงเงื่อนไข; ไม่มี quota transfer ที่ยืนยัน |
| ChatKit/API | server API key; billing Platform [S4 เดิม,S6] | ไม่เลือกเป็น paid fallback |
| App Server | product integration, managed browser/device-code login, stream; command/WS experimental ไม่รองรับ production [S5] | candidate; device-code ช่วยประเด็น callback แต่ไม่พิสูจน์ runtime Android/iOS |
| ChatGPT Remote | งานรันบน Mac/Windows ที่เชื่อมต่อ [S11] | ไม่ตรง no-companion และบทสนทนาใน LexiQuest |

**Fact:** Free มีใน pricing [S7]; ChatGPT-auth กับ API-key เป็นการเข้าถึงคนละ billing [S6]. **Inference:** App Server ควรตรวจต่อ ไม่ควรตัดทิ้งเพียงเพราะชื่อ Codex [S5,S10]. **Unknown:** U1 สิทธิ์ integration ของ Free ไทย; U2 runtime/transport/login return บนมือถือหรือ topology server ที่แยกเจ้าของ; U3 บังคับไม่ใช้ paid credits/overage; U4 distribution/revoke/isolation. แนวคิด hosted App Server อาจไม่ต้องมีคอมของผู้เรียน แต่ยังไม่ได้รับรองและมีต้นทุน hosting แยก ส่งให้ V1 ประเมินเท่านั้น

ไม่มีบัญชี Free จริง จึงไม่ login ไม่ใช้บัญชีนักพัฒนาแทน ไม่ยิง paid API ไม่ดึง token ไม่รัน tests เดิม ไม่แก้ source/schema. T01–T08 และ pilot ยัง NOT_RUN. ไม่มีหลักฐานใหม่ปิด U1/U2 หลังค้นเฉพาะช่องว่าง จึงหยุดค้นซ้ำ; ไม่ใช้ความเงียบของเอกสารเป็นหลักฐานว่าเป็นไปไม่ได้

แหล่งข้อมูล (retrieved 2026-09-20; S1–S4 ใช้ evidence เดิม; S5–S7/S10/S11 เปิดตรวจใน V0):
- S1 [MCP resource authorization](https://developers.openai.com/plugins/build/auth)
- S2 [Sites identity](https://learn.chatgpt.com/docs/sites)
- S3 [Partner identity and tool grants](https://learn.chatgpt.com/docs/plugins)
- S4 [API-backed chat session](https://developers.openai.com/api/docs/guides/chatkit)
- S5 [Auth endpoints; 3b device-code; protocol; experimental transport](https://learn.chatgpt.com/docs/app-server)
- S6 [OpenAI authentication; API billing separation](https://learn.chatgpt.com/docs/auth)
- S7 [Free inclusion and shared usage](https://learn.chatgpt.com/docs/pricing)
- S10 [Embedded harness, not blanket mobile entitlement](https://developers.openai.com/blog/codex-as-a-platform) — published 2026-08-19
- S11 [Connected computer requirement](https://learn.chatgpt.com/docs/remote)

รายละเอียด route, sources, unknowns และ evidence paths อยู่ `V0-handoff.json`. nextPhase = V1; V1 ตัดสิน gate ร่วม V0+V1 และส่ง V3 ถ้าไม่ผ่าน ห้ามสร้าง prototype เพื่อลดเงื่อนไข. ขอ model gpt-6-astra / medium / Standard-default; effective runtime tier ยัง UNCONFIRMED.

