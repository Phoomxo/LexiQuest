# R3-04 — Context และ reviewed coordinator

ปิด package ด้วย coordinator fallback ตาม execution-r3; live dispatcher ยังไม่เปิด.

04.1 ทดลอง GODKILLER binary ที่ตรวจ hash แล้วกับ fixture สังเคราะห์เท่านั้น: repo-map เก็บ symbol แต่ขาด source/owner/repro/acceptance ทั้ง6 markers; prune เก็บ6/6แต่ขนาดไม่ลด. ใช้ canonical reads. ไม่เปิด hook/proxy/purify และไม่อ้างผล Antigravity A/B หรือ token savings.

04.2 เพิ่ม offline review journal ต่อกับ model_route เดิม ไม่มี process launcher. ตรวจจริง read-only1783pins ไม่มี diff; route DETERMINISTIC_FALLBACK, actual coordinator metadata Astra/medium, commands ใช้cwd d38e. App task cwd c0bcไม่ใช่ source; tierไม่เผย. ไม่มี worker/Jev call.

04.3 หนึ่งactive packageต่อaudit DB; source/cwd/model/effort/tier guards; scope diff และnonzero exitเข้าRECOVERY; review+evidenceก่อนVERIFIED; incident IDs/attemptsรักษาข้ามrestart. Journalเป็นlocal auditจากข้อมูลที่coordinatorตรวจ ไม่ใช่OS sandbox, distributed lease หรือevidence oracle. ทดลองงานเอกสารเสี่ยงต่ำ sequentialจริง:แก้READMEเพียงไฟล์เดียว ตรวจdiffและclosure แล้วreview.

Targeted verify-scopeผ่าน55 offline testsหลังREDและแก้source-binding/missing-cwd/blank-reason. ไม่rerunหลังREADME-onlyเพราะPython/runner/verifier closureเดิมครบ. Sourceเปลี่ยน3ไฟล์เฉพาะtool/experiments/jev_review;แอป/อุปกรณ์/ledgerไม่เปลี่ยน. เก็บเหตุrecover encodingและselectorในreceipt.

หลักฐาน: [receipt](r3/receipt-r3-04.json), [55 tests](r3/verification-r3-04.json), [A/B](r3/context-ab-r3-04.json), [dispatch audit](r3/dispatch-audit-r3-04.json), [source pins](r3/source-manifest-r3-04.json).

ยังpending: liveChoice/freshbilling/liveworker binding, providerA/B efficiency, R3-02 provider/owner lifecycle,14/14และoriginalFreeV2. ส่งต่อR3-05ปิดcriteriaโดยคงexternal prerequisitesจริง.
