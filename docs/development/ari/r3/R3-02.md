# R3-02 device and restore result

02.1 and02.3 complete. 02.2 native catalog, search/clear, combined food/A2 empty filter, exact r1/r2 detail and no-AI baseline verified on Vivo using installed APK116. Actual provider semantics and owner/reconnect lifecycle remain external pending: app reports unpaired AI. No whole-app acceptance.

The first native detail failed because the synthetic fixture owned words under an inactive separate owner. A real Drift reader regression reproduced VocabularyNotFoundFailure. The test-only fixture now reuses an existing active owner without modifying owner records or loosening production access. Regression verifies readable words and rejection after switching owner; five targeted tests passed including canonical working-clone emission. Production source/APK unchanged. Existing116 catalog/detail receipt dependency closures remain unchanged and were not rerun. Two-file analyzer reports one pre-existing info for transitive path import; no errors/warnings.

Original isolated main/WAL/SHM restored byte-exact after each native run. WAL replay integrity and all58 table counts matched. Restored relaunch compared all rows:56 tables exact; only content_download_states.updated_at and runtime_flags.updated_at/expires_at changed. Final device database restored byte-exact and app force-stopped. Original application/accounts untouched. Preserve original backup and adoption ZIP.

Transfer incident: exec-out stdin stalled with zero-byte destination before app launch; stopped owned processes and used push/run-as copy plus SHA256 verification. Initial fixture edit failed due Windows cp874; corrected explicit UTF-8. A redundant RED run occurred after format-only change; subsequent corrected-source gate passed. No hidden PASS.

Source/receipts: source-manifest-r3-02.json, device-receipt.json, fixture-verification-r3-02.json, native-r3-02/. R3-03 is the next independent package; R3-02 pending items stay owned in the ledger for revisit. No Jev call, spend, deployment or research. Route is deterministic fallback, not Jev selection.
