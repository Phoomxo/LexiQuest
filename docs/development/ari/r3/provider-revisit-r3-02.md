# Remaining R3-02 provider/lifecycle protocol

Owner: R3-02 pending slice, scheduled with R3-05. Evidence is external pending, never whole-scope PASS.

Prerequisite: fresh connected/unlocked Vivo observation and an authenticated paired optional debug provider session; app currently reports unpaired. Prior process-death recovery required private user login. Do not infer entitlement from USB/MCP or call paid alternatives. Original Free/no-companion gate remains NOT_MET.

Recheck exact source/APK and isolated package; retain original backup. Force-stop and take a fresh main/WAL/SHM snapshot before any fixture installation. If current DB differs, preserve the new state and regenerate the fixture from a clone; never overwrite new learner changes with the older snapshot. Test fixture uses the existing active owner and canonical checksums. Never touch original application/accounts.

On catalog and r1/r2 detail, capture actual provider-bound context and response: search/filters, counts, pinned revision and zero practice are consistent with visible screen. Ask for interpretation of pack level and progress; pack level must not become learner proficiency and opening must not be completion. Exercise late attachment, same-owner reconnect, changed owner, background return and disposed route. Verify stale context/commands cannot execute for the wrong owner or route. Keep host/native/provider/storage results distinct.

Restore the exact snapshot taken for that future run, compare all three files before launch, then compare durable rows after relaunch with narrowly documented operational timestamp changes. Record owner, source/fixture/build hashes and receipts. No fixture remains installed after testing. Existing final restore in device-receipt.json is proven and must not be relabeled pending by this future protocol.
