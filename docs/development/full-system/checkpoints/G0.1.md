# G0.1 — Worktree, authority and writer bootstrap

Plan revision: `2026-09-13-sequential-3`; package `P0.1`, ordinal 1.
Owner task: `01a098cf-4917-7472-a705-a4eff94a6e30`.
Worktree: `C:/Users/Phet/.codex/worktrees/c233/LexiQuest`.
Branch: `codex/g0-1-bootstrap`. Requirements: PLAN-01 and G0 (partial phase coverage).

## Accepted scope and source decision

The native checkout started clean and detached at main/planning HEAD
`7b8ac6cc029c0df43f9d4e7d161da3502e6f557b`. All three inspected worktrees share
`C:/Users/Phet/Documents/LexiQuest/.git`. This isolated branch starts from the
verified R15 implementation HEAD `f4eebb895836349fbf8e9960312a78bad524d462`.
R15 supplies the existing implementation needed by G0.2; this is a bootstrap
base decision, not acceptance of every R15 behavior or rejection of main fixes.
Git reports 58 main-only and 382 R15-only commits. G0.3 owns their semantic
reconciliation. No merge of the two histories was performed.

The 7712 planning checkout remains a separate document pin. Its 81 approved
seed paths were copied byte-for-byte after SHA-256 and collision checks.
The seed manifest SHA-256 is
`1155fd1f86b19fb0305253baf2c102ba23f51a10f7b5985f3351a271773c6eca`.
Seven existing paths were reviewed: AGENTS and the R15 workflow are replaced by
current authority; four R15 reference documents gain supersession headers;
the evidence JSON has only line-ending differences. Prior versions remain in
R15 Git and the untouched source worktree. The other 74 seed paths are additions.
Successors consume the accepted commit, not the old 7712 seed again.

## Evidence and ownership

- [Seed manifest](../evidence/G0.1/bootstrap-manifest.json),
  [source/dirty inventory](../evidence/G0.1/source-manifest.json), and
  [per-path collision/restore dispositions](../evidence/G0.1/collisions.json).
- [Verification](../evidence/G0.1/verification.json): 81 seed hashes matched,
  six repair input hashes matched, 517 relative document links resolved.
  [Link details](../evidence/G0.1/links.json).
- Related tasks `01a0985f-5b0d-7521-8335-a4c906a2dd0c` and
  `01a09711-4956-7b10-b4c8-d9f9fb3e1af1` were confirmed idle using app snapshots.
  `read_thread` confirmed this task's exact title, active status and c233 cwd.
  Control state records this task as the sole writer after receipt reconciliation.
- Source842c has six repair changes, seven generated registrant changes,
  a changed historical checkpoint and two new repair documents; none were copied
  or edited here. [Repair pins](../evidence/G0.1/repair-input-pins.json) are input
  identity only. G0.2 owns acceptance, generated-file disposition and evidence reuse.

No application implementation, Flutter/backend tests, Android build, GPU check,
full verifier, research operation, deployment or remote Git write was performed.
No previous runtime PASS is promoted to a fresh result. Bounded source inspection
is the package's specified verification kind; verify-scope is not needed for docs.

## Operational defects and recovery

G01-OPS-01: initial receipt read expected a dispatched receipt but found no file.
Cause: parent dispatch metadata was still being written during native setup.
After checking existence, the setup-pending receipt appeared; current app identity
was verified and reconciled atomically. No create retry occurred. Closed.

G01-OPS-02: an initial wait request included the calling task and was rejected.
Cause: wait_threads prohibits self-waits. Separate read_thread confirmed own
identity; bounded waits confirmed both source tasks idle. Closed; no blind retry.

G01-OPS-03: initial control read included too much metadata and was truncated.
Subsequent inspection used parsed JSON projections and only P0.1/P0.2 entries.
No truncated output was used as a parsed manifest. Closed.

G01-OPS-04: default staged whitespace check flagged four existing Markdown
hard-break lines (master plan lines 5–8, each ending with two spaces). Preserve
the approved seed hashes. The adjusted check disables only blank-at-eol for
that master-plan path; all other staged files retain the default check. These
four intentional hard breaks are accepted, not an application defect.

## Handoff and remaining gates

Accepted source SHA is recorded after commit in the external control handoff:
`C:/Users/Phet/.codex/visualizations/2026/09/13/01a09888-61dd-7680-9b19-c33035043d59/full-system-orchestration/handoffs/G0.1.json`.
Only seed paths and this package's evidence/checkpoint are committed. Git staged
scope/diff checks and post-commit clean status are recorded in that handoff.
Rollback: inspect/revert this bootstrap commit only on an isolated branch; prior
source is the R15 SHA above. Do not reset or clean either source checkout.

G0.1 acceptance is limited to source/authority/ownership bootstrap. G0 remains
in progress through G0.8. G0.2 accepts F1–F4; G0.3 reconciles histories; G0.6/G7.4
own actual cleanup. The final order remains G0–G7 development, G8.2 whole-app
review, G8.3 repairs, G8.4 System Test Plan, execution/fault cases, defect/retest,
then G8.9 release ledger. Release this writer before reserving one G0.2 task.
