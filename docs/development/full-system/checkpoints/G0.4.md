# G0.4 — Bounded verifier acceptance

Plan `2026-09-13-sequential-3`; P0.4; PLAN-02, PLAN-11, G0 partial.
Task `01a098eb-7ee3-7182-ae8b-7c1486cb5db9`; isolated worktree
`C:/Users/Phet/.codex/worktrees/3fa1/LexiQuest`, branch
`codex/g0-4-bounded-verification`. Clean native detached main was
`7b8ac6cc029c0df43f9d4e7d161da3502e6f557b`; common Git directory verified as
`C:/Users/Phet/Documents/LexiQuest/.git`. Created own branch from accepted
G0.3 `dcd8c97b2c71de69b897eb89c92a1a2323bc5be7`. Read-thread confirmed own
identity and setup-pending receipt was reconciled before claiming vacant writer.
No source d7fe/293e/c233/842c/7712 writes, reset, clean or background workers.

## Accepted output

[Contract/design and registry disposition](../G0.4-bounded-verification.md).
Runner adds environment/tool/config pins, backend runner closure, complete input
inventory, log-hash and exit validation for reuse, plan-only, CLI-only and a
clean frozen-HEAD release guard. Missing command paths/scripts fail before any
suite starts. Existing F2 frontend selection and closure remain; new regression
is the necessary owner-path expansion. Generator was inspected, left unchanged.
Only runner, new CLI regression, ledger and package documentation/evidence change.

[Verification](../evidence/G0.4/verification.json) records exact source and
environment fingerprints, 1,325 closure rows, two passing CLI scripts and preserved
logs. Final real `-Resume` skipped both commands with identical pins; it did not
execute them again. Tests cover exact skip, source/environment changes, missing
logs and bad prior exit, backend closure and plan-only zero child execution.
The registry audit accounts for 20 entries and ten expected parameter/path
rejections. No full release command was started.

Review checked source/command identity, explicit target isolation, failure-before-
dispatch, result version compatibility, log retention, secret-free environment
digests and consumers. Source pins and copied logs are checked after the final
gate. G0.2 application and G0.3 source-inspection evidence is retained as prior
accepted evidence, not rerun or promoted to current v2 runtime PASS. Wrapper
fingerprints include HEAD/raw EOL, so the final commit's SHA differs from tested
HEAD; source manifests preserve what was actually tested.

## Defects and dispositions

- G04-OPS-01: initial control/source output truncated. Replaced broad output with
  parsed projections and exact excerpts; complete on-disk JSON is authority.
- G04-OPS-02: brief-listed `verify-scope.tests.ps1` did not exist in accepted
  source. File inventory confirmed absence; created meaningful behavioral coverage.
- G04-CLI-01: RED reproduced backend runner omission for all three backend areas.
  Added runner/root configuration closure plus environment/tool/config digest;
  final bounded regressions pass.
- G04-CLI-02: registry audit found missing Economy/Integration paths. Added
  pre-dispatch guard; ten rejection probes pass. Actual missing production
  implementations remain required P7.3 and P7.7/P8.4 obligations in ledger.
- G04-OPS-03: first rejection probe stopped on expected native stderr because the
  harness used Stop. Scoped Continue only around expected child failures, retained
  exit checking; all ten probes completed without executing their suites.
- G04-OPS-04: PowerShell Select-Object projection of an ordered dictionary and
  string Parse on decoded timestamps produced null display/duration fields.
  Re-read serialized JSON as objects and used typed DateTimeOffset subtraction;
  final evidence validates positive durations and nonempty fingerprints.
- G04-OPS-05: initial explicit staging rejected four evidence logs under the
  repository's global log ignore rule. Retained raw logs and force-added only
  those four reviewed evidence paths; no ignore policy or unrelated file changed.

## Remaining gates and handoff

Economy functions package/root emulator script and Integration test/device runner
are unavailable, not passing. Full Flutter/backend/Android/GPU/release and real
device/live/human gates NOT RUN here. Environment caching is conservative, not a
hermetic guarantee for manually corrupted external dependency caches.

Commit reviewed accepted allowlist only. Full accepted SHA and source/config/log
pins are in external `handoffs/G0.4.json` under the creation-prompt control path.
Rollback: review/revert this isolated package commit; original worktrees retained.
G0.4 accepted; G0 remains in progress. Release writer, reserve/create G0.5 only,
persist receipt, inspect one progress snapshot and stop source writes. G0-G7
development precedes G8.2 review, G8.3 fixes, G8.4 System Test Plan, tests/journeys/
faults, defect/retest and G8.9 release ledger.
