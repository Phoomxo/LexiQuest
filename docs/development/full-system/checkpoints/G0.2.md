# G0.2 — F1–F4 acceptance and generated-file disposition

Plan `2026-09-13-sequential-3`; package P0.2, ordinal 2. Requirements PLAN-02,
PLAN-03, G0 (partial). Task `01a098d4-e0be-7362-bfcb-b9fe03aac80a`.
Worktree `C:/Users/Phet/.codex/worktrees/293e/LexiQuest`, branch
`codex/g0-2-repair-acceptance`. Initial native HEAD was clean/detached main
`7b8ac6cc029c0df43f9d4e7d161da3502e6f557b`; accepted predecessor/base is
`a66285dae472045b9bffd215f55c505a535de7ff`. Common Git directory was verified as
`C:/Users/Phet/Documents/LexiQuest/.git` before creating this isolated branch.

## Accepted changes and review

The six files in [repair-manifest](../evidence/G0.2/repair-manifest.json) match
the original six-file manifest and G0.1 pins byte-for-byte. No F1–F4 fix was
implemented again. Reviewed the complete source/test diff against R15
`f4eebb895836349fbf8e9960312a78bad524d462`, reset/settings/dispose callers,
speech session identity guards, verifier command selection, and camera CLI and
trainer consumers. Package review found no acceptance-blocking issue.

- F1 retires speech callbacks synchronously and fences final/error/status/start
  completion by attempt, conversation and session; settings awaits the existing
  cancellation. Regression assertions remain intact, including cancel failure.
- F2 broadens input hashing without widening targets; Runtime points to the
  existing CLI contract test. Actual Git fixture mutations exercise seven areas
  and eight dependency paths plus unchanged-input stability.
- F3 recomputes pinned validation predictions/metrics before test evaluation;
  incomplete old freezes fail closed. The test-opened receipt stays conservative.
- F4 requires pinned raw-label mapping and a boolean acceptance decision; baseline
  unknown acceptance no longer depends on membership in the four-class vocabulary.

Accepted the original repair plan and verification report as historical repair
explanations. Their 842c/old worktree statements describe that run, not current
writer authority. The old changed R15 checkpoint was preserved separately rather
than overlaid on current authority; see [disposition](../evidence/G0.2/historical-checkpoint.json).
No schema, dependency, shipped-model, canonical learning/reward or research change.

## Verification and evidence reuse decision

[Verification record](../evidence/G0.2/verification.json),
[1293 input pins](../evidence/G0.2/flutter-inputs.json),
[environment/config](../evidence/G0.2/environment.json), and
[upstream RED/GREEN evidence](../evidence/G0.2/upstream-evidence.json).

The original wrapper fingerprint was reconstructed exactly as
`abac5b94eee1ce55e460eb86ab7f1c8142cfb7d35206de1a98c3dceb20ec990b` on 842c.
This checkout differs only in line endings of four other Flutter inputs; with
the original Git pins its fingerprint is
`5ec25b06dc55d10d98996a018066189b478ef636f83e550cdf32c56eca25fcf0`.
Therefore the old 155-case result was not reused as current PASS. Python/CLI old
logs lack complete runtime pins, so their bounded modules were also rerun.
No passed unchanged current gate was rerun.

Fresh verification on the six accepted files:

| Gate | Actual result |
| --- | --- |
| verify-scope Targeted/AI, same 11 explicit targets as repair report | 155 passed, exit 0 |
| PowerShell r15-scope contract regression | Passed, exit 0 |
| Python camera evaluator module, synthetic fixtures only | 20 passed, exit 0 |
| Flutter analyzer, two touched Dart files | No issues, exit 0 |

Current wrapper fingerprint:
`5c1f005f74493ca3e5c2191630f95dbc1ff9f7ca0cb8da7af1fb3c39a4050d3b`.
The wrapper contains the predecessor HEAD plus working-file hashes; the final
commit changes only documentation/evidence beyond those tested inputs. The final
input audit ties every tested file to the accepted output without pretending
the wrapper originally ran on the later commit SHA. No lockfile change occurred.

## Generated files and operational defects

[Seven registrant dispositions](../evidence/G0.2/generated-disposition.json)
include raw hashes, immutable Git blob comparisons, and external backup paths.
All seven are Git-clean-equivalent line-ending changes. Original 842c files were
left untouched. Offline dependency setup regenerated the same seven in this
checkout; their raw copies were also preserved before restoring only those
verified generated paths to base content. They are excluded from the commit.

G02-OPS-01: oversized early reads were truncated. Switched to bounded sections
and parsed projections; no partial JSON was used as evidence. Closed.

G02-OPS-02: list_threads(limit=100) returned a validation error and the attempted
JSON parse failed. No create retry occurred. read_thread on the environment's
task ID verified actual G0.2 identity/cwd, then the setup-pending receipt was
reconciled atomically. Both source tasks were individually confirmed idle. Closed.

G02-OPS-03: rg with a Windows wildcard embedded in a literal path failed once.
Used exact discovered document paths; no retry of the failing form. Closed.

These were tooling/read errors, not behavioral test failures. All current gates
passed on their first run. Historical RED and the original settings failure are
retained with their successful repair evidence; no historical result is relabeled.

G02-OPS-04: verification summary initially parsed JSON DateTime objects through
culture-sensitive strings, producing an incomplete gate list. Replaced that
conversion with typed subtraction and rebuilt the list with terminating errors;
validated all four gate records. No tests were rerun. Closed.

G02-OPS-05: staged whitespace check flagged analyzer progress-line padding in
the raw log. Preserved the log unchanged; scoped whitespace validation excludes
raw log payloads while still checking all source, tests, JSON and Markdown. Closed.

## Handoff

Accepted full SHA is recorded after commit in external `handoffs/G0.2.json` under
`C:/Users/Phet/.codex/visualizations/2026/09/13/01a09888-61dd-7680-9b19-c33035043d59/full-system-orchestration`.
Rollback: review/revert this package commit only in an isolated branch; original
repair source, raw registrants, historical checkpoint and base Git objects remain.

G0.2 accepted; G0 remains in progress. G0.3 owns main/R15 semantic reconciliation
(58 main-only / 382 R15-only commits), using current accepted docs, not old 7712
seeds. Full release, backend, Android/device, live AI/voice, physical camera and
human acceptance remain NOT RUN here. One G0.3 successor may start only after
writer release and durable handoff. The final sequence remains G0–G7 development,
G8.2 whole-app review, G8.3 fixes, G8.4 System Test Plan, tests/journeys/faults,
defect/retest, then G8.9 release ledger.
