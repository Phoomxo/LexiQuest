# G0.3 — Semantic reconciliation acceptance

Plan `2026-09-13-sequential-3`; P0.3, ordinal3; PLAN-01, G0 partial.
Task `01a098de-76b8-7132-9cc1-87c600ee8b63`; own worktree
`C:/Users/Phet/.codex/worktrees/d7fe/LexiQuest`; branch
`codex/g0-3-semantic-authority`. Native initial HEAD was clean/detached main
`7b8ac6cc029c0df43f9d4e7d161da3502e6f557b`. Verified common Git directory
`C:/Users/Phet/Documents/LexiQuest/.git`, then created branch from accepted
G0.2 `e1b3fcb17fbdbdab158780142383f3ee15d8da0a`. No source293e/c233/842c/7712
writes. Own identity was confirmed with read_thread; setup-pending receipt
was reconciled to actual task ID before claiming the vacant writer reservation.

## Result and review

[Authority decisions](../G0.3-authority.md) and
[semantic ledger](../evidence/G0.3/semantic-ledger.json) account for all
48 main non-merge + 382 R15 non-merge commits; main's 10 additional merge
nodes are explicitly excluded. Census covers 4,886 commit/path deltas across
1,603 paths, including tests, docs, generated files, native/dependencies and
research history. Every delta has disposition, owner and pinned source evidence.
No merge/cherry-pick, application change, schema migration or dependency update.

Selected R15/G0.2 cumulative source, one AppBootstrap, one AppDatabase schema26,
canonical evidence/local reward ledger. Main is a selective donor. D01-D12
close authority choices and preserve concrete obligations in the work ledger.
Source-inspection acceptance is distinct from runtime verification.

Material findings retained as required owner work:

- P1.7: main lazy-SDK construction invariant and safe-open/future-schema/integrity
  protection must be checked/ported into current composition; old learning storage
  files are empty and do not provide protection. P1.4/P7.2 own legacy import.
- P7.3: missing-provider fail-open and legacy client points/purchase permissions
  differ from main lockdown; keep versioned field_users compatibility. Main
  callable transaction is not proof of correct answers. Remote integrity remains open.
- P7.6/P7.7: root npm uuid and voice GPU dependency differences remain unresolved
  implementation work; release offline/signature/artifact gates must be reconciled.
- P7.3/P7.5: storage policy differences require forward migration/activation review.
  Cloud sync currently defaults true; no local-only configuration was assumed.

G0.8 must carry these into edition/development gates. They are not G0.3 runtime
fix claims. Historical research/physical/live results are not current PASS.

## Verification

[Verification record](../evidence/G0.3/verification.json) contains actual commands,
durations, source/config/artifact fingerprints, completeness and path/link checks.
[Reproducer](../evidence/G0.3/inspect_git.py) reconstructs the census directly from
pinned Git objects. [Verifier](../evidence/G0.3/verify_inspection.py) independently
checks commit/path coverage and object pins against those objects.
Source-inspection does not have a verify-scope runtime target; inspected current
wrapper, and used Git/JSON/hash/path/link checks as required by own brief.
G0.2 accepted repair file blobs remain unchanged. No unchanged passed gate rerun.
Flutter/backend/Android/GPU/full release/physical/live/human gates NOT RUN here.

Review checked authority against current callers, schema and migrations, local
reward writer, remote rules and main callable/store contract, native identity,
dependency pins, storage SQL and release workflow deltas. Historical R15 commits
are adopted cumulatively; the census is not a claim of manually testing 382
individual intermediate states. No known acceptance-blocking source-disposition
gap remains after complete owner mapping and validation.

## Operational defects

- G03-OPS-01: first control-state read and two source batches exceeded output
  budget. Expected complete bounded output, actual truncation; no partial JSON
  used as authority. Fixed with local parsed projections and smaller exact source
  excerpts. Full on-disk ledger/JSON verification is the retest evidence.
- G03-OPS-02: one rg command included nonexistent `backend/lm_api/pyproject.toml`.
  Expected backend pins, actual path error. Discovered actual
  `backend/lexiquest_lm/pyproject.toml` with rg --files; did not repeat the failing
  path or interpret absent output as a clean dependency gate. Voice pin comparison
  used the existing voice_api path and Git diff. No filesystem mutation involved.
- G03-REVIEW-01: initial path mapping left 73 paths assigned to inventory triage,
  including lowercase AGENTS matching and runtime/media/review helpers. Replaced
  broad fallback with explicit consumer owners and regenerated changed inspector
  output. Also restricted main Android exact-equivalence to matching blobs.
  Completeness verification requires zero residual fallback paths.

## Handoff

Commit only accepted docs/evidence/ledger after source-inspection verification.
Full output SHA is external in `handoffs/G0.3.json` under the control directory
from the creation prompt. Rollback is review/revert this isolated docs-only
commit; application bytes, original worktrees and Git pins are retained.
G0.3 accepted, G0 in progress. Release writer, reserve/create G0.4 only once,
record actual receipt, inspect one progress snapshot, then stop source writes.
G0-G7 development remains before G8.2 review, G8.3 fixes, G8.4 System Test Plan,
tests/journeys/faults, defect/retest and G8.9 release ledger.
