{
  "scope": "Explicit full source reads; cross-check pending, not zone acceptance",
  "notes": {
    "lib/features/learning/application/lesson_mode_registry.dart": "Full246 lines read. Unique mode/routes and feature-consistent shared production entry indexing; typed recall uses exact quiz route authority. Resolve filters implementedOff while raw find supports readiness. Pair and scratchpad default implementedOff; registry metadata still requires live feature gate at invocation.",
    "lib/features/learning/application/legacy_lesson_mode_adapters.dart": "Full64 lines read. Compatibility adapters reject recreational evidence before active-effort classification; next is unsupported. Legacy registry limited to original reading/quiz/SRS plus typed recall; production uses separate buildLessonModeRegistry.",
    "lib/features/learning/application/hint_use_cases.dart": "Full23 lines read. Stateful wrapper delegates staged validation to HintPolicy; reset only explicitly named postcommit operation. Controller resets after acknowledged result publication; shell caller ordering still pending.",
    "lib/features/learning/application/session_configuration_policy.dart": "Full481 lines read. Exact consent/assignment protocol binding, baseline only in legacy mode, capability/limit validation and intersection, pack allowlist, matching-only density, clamped initial selection then exact revalidation. Live digest revalidation prevents silent changes to accepted configuration. Runtime feature gate remains external to registry delivery check.",
    "lib/features/learning/application/unified_lesson_controller.dart": "Full2037 lines read. Serialized mutations with pending semantic fingerprints, feedback validation before write, explicit terminal retry authority, postcommit hint reset, Pair purpose excludes learning time, monotonic configured effort and generation-fenced timers. start attaches existing durable session rather than creates one. UTC checks do not reject wall-clock reversal, supporting terminal-time candidate. dispose cancels clocks but does not drain mutation tail; outer shell lifecycle must settle admitted work before disposal. Config cutoff and captured evidence freeze caller ordering remain cross-checks."
  }
}
