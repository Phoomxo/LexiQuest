{
  "bundle": "B12",
  "baseSha": "93eb652a1a550e496262cba4e7b5602da84ede8e",
  "design": "Preserve the four-class F3/F4 fresh protocol. Add a separate, explicitly development-only 16-class evaluator with no fresh-test or rollout authority. Reuse accepted acquisition/training gates via byte pins; recompute predictions, metrics and freeze integrity. Missing unknown/camera evidence remains missing.",
  "alternatives": ["Changing the legacy four-class schema would reinterpret accepted freezes; rejected", "Reporting existing training accuracy alone omits threshold and integrity contracts; rejected", "Separate versioned development freeze plus retained legacy protocol; selected"],
  "execution": "Single writer inline using executing-plans and TDD; no repeated plan approval under explicit successor authorization",
  "packages": [
    {"id": "G5.4", "status": "in_progress", "files": ["tools/camera_development_evaluation.py", "tools/test_camera_development_evaluation.py"], "steps": ["Test exact validation ID closure, finite normalized scores, label identity, disjoint provenance metadata, development-only status and freeze tampering", "Implement pinned file loader and exclusive freeze CLI; retain complete predictions and recompute all derived fields", "Pin actual manifest/export/model/predictions and accepted curation receipt; run CLI on actual 120 candidate predictions", "Record minimum30/class and200unknown fresh coverage gaps; do not open fresh test", "Review, commit and store actual SHA before G5.5"], "testCommand": "C:/Users/Phet/.cache/lq12-20260911/Scripts/python.exe -m unittest discover -s tools -p test_camera_development_evaluation.py -v"},
    {"id": "G5.5", "status": "pending", "steps": ["Read own brief and inspect metric/resource callers", "Test and implement per-class/known rejection/unknown false acceptance and Wilson intervals with zero denominator null; explicit paired baseline mapping and acceptance", "Measure actual candidate host inference/resource evidence only where supported; keep physical and unmeasured comparisons NOT RUN", "Validate immutable freeze and actual model; record threshold0.6 as fixed descriptive policy, not calibrated unknown gate", "Review and commit accepted metrics and resource disposition"]},
    {"id": "G5.6", "status": "pending", "steps": ["Read own brief and assemble all G5 source gates without rerunning unchanged passes", "Verify baseline source/hash/rollback pin and candidate releaseEnabled=false; retain baseline for incomplete camera/open-set/vocabulary gates", "Use verify-scope.ps1 for affected manifest/runtime tests if no reusable evidence; verify existing gate input closure", "Record concise Thai bundle report and JSON acceptance/review/defects", "Commit, durable handoff, release writer and reserve exactly one B13 under lock, create native successor and save receipt immediately"]}
  ],
  "invariants": ["67packages/21bundles", "lexiquest/ only", "no fresh-test tuning", "no production model replacement", "no paid compute or deployment", "host results do not satisfy physical gate", "Astra medium default requested; effective tier unverified"]
}
