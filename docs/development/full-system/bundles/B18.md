# B18 · Freeze และ whole-app review

G8.1: ตรึง source `b0321b3416ca67c9098644f5df59d353071cfdc3` บน `lexiquest/b18-freeze-review`; application source หยุดเขียนระหว่าง review. [Inventory](../evidence/bundles/B18/G8.1-inventory.json) ครบ 3,610 tracked files: handwritten 1,331 / configuration 90 / generated 8 / assets 34 / reference 2,147. B18 tooling ใหม่อยู่ใน untracked disposition และต้องตรวจ delta ก่อน final freeze.

[Acceptance](../evidence/bundles/B18/G8.1-acceptance.json) ตรวจ accepted packages ก่อนหน้า 58 ข้อ, retained artifacts B15/B16/B17 จำนวน 106/115/85 และ candidate/dataset/config pins ตรง bytes. คืนเฉพาะ checkout EOL transformations 245 ไฟล์ในการตรวจครั้งแรก; ครั้งถัดมาตรวจตรงทุกไฟล์. ไม่มี production change หรือ runtime test ใหม่จาก inventory.

G8.1 accepted commit `6c24330bc1c5dd11519504e52a4998c70df6e605`; external receipt บันทึกแล้วและ run-state revision123 อยู่ G8.2.

G8.2 **ยังไม่ผ่าน**: [review ledger](../evidence/bundles/B18/G8.2-review-ledger.json) มีบันทึกการอ่าน REV-01 จำนวน41ไฟล์และ dependency cross-checks; ต่อเนื่องมาที่ REV-02 โดยอ่าน schema/guards, history/replay, evidence codecs/policy/capture, learning use cases, canonical repository และ projection rebuilder แล้ว. [บันทึก REV-02 รายไฟล์](../evidence/bundles/B18/review_observations_rev02.json) ผูกกับ source hash และยังรอตรวจ callers/oracles; ไม่ใช่การรับรองโซน. Handwritten source รวม484,361บรรทัด; ไม่ใช้ inventory/count/hash แทน semantic review.

[Findings](../evidence/bundles/B18/defects.json) มี17ข้อ: cleanup registration ใน bootstrap, ปุ่ม retry ที่หายไปเมื่อข้อสุดท้ายถูกข้ามแล้วปิดกิจกรรมล้มเหลวใน definition/cloze, feedback ของ Pair ที่ไม่หายหลังย้าย owner การกู้คืน mixed-review ช่วงปิดงานที่สูญเสียสถานะ/ปุ่ม retry และ reset-password form ที่หายเมื่อกรอกรหัสผ่านสั้นเกินไป และหน้าลูกเพิ่มคำศัพท์ที่ยังบันทึกได้หลัง emergency-off ยืนยันจาก control flow; candidate เดิม5ข้อยังต้อง reproduce: guest consent/re-entry, stale circuit completion, legacy WAL allocation, terminal time เมื่อ clock ถอยหลัง และ generic session admission เมื่อ owner เปลี่ยน. เพิ่ม B18-REV09-01: lost-ACK replay ใช้ revision ล่าสุดตรวจใบตอบรับเดิมจน actual Firestore codec ปฏิเสธ; fake ใน regression เดิมข้าม codec. เพิ่ม B18-REV12-01: schema transport oracle ยังคาดว่า27ถูกปฏิเสธ แต่ current schema/allowlist รองรับ27แล้ว; ต้องแก้ stale oracle โดยคง negative cases ของรุ่นไม่รองรับ/ชนิดผิด. เพิ่ม candidate B18-REV09-02: bounded proof scan อาจติดหน้าแรกที่ทุกแถวถูกปฏิเสธและไม่ตรวจข้อมูลที่มีสิทธิ์ในหน้าถัดไป ต้อง reproduce โดยคง no-unauthorized-write invariant. ไม่มีข้อใดถูกอ้างว่าปิดแล้ว.

[REV-03 observations](../evidence/bundles/B18/review_observations_rev03.json) เพิ่ม lifecycle/shell และ adapters, lexical identity, definition/cloze UI; [test-oracle observations](../evidence/bundles/B18/review_observations_rev12.json) แยกการอ่าน test source ออกจาก runtime results. [Recovery delta](../evidence/bundles/B18/recovery-policy-delta.json) ยืนยัน schema2: แก้วิธีค้น path แล้วทำต่อใน writer เดิม; AGENTS/workflow ปัจจุบันสอดคล้องอยู่แล้ว จึงไม่แก้ frozen source หรือประวัติย้อนหลัง.

Source ฝั่ง Pair, accessibility และ Adventure ทั้ง domain/application/data/presentation อ่านครบพร้อม observations แล้ว; เพิ่ม Adventure recovery/controller/widget/domain/catalog/bridge/composer, entry/journey/repair/projection/diagnostics และ file-backed restart test oracles. Caller/test oracle ข้ามระบบยังไม่ครบ. Ledger ปัจจุบัน970 full-source observations +4 in-progress review,455 pending; ไม่ใช่970 runtime passes. เพิ่ม companion, vocabulary/editorial/starter catalog และ pack repositories พร้อม oracles; recommendation/Active Recall กับ tests และ sync engine/gate/contracts/Workmanager/Firestore gateway อ่านครบ เพิ่ม sync store6,575บรรทัด พร้อม Firestore codec/preflight/store oracles แล้ว; sync engine/research transport adapter/preferences/reward/SRS/reminder/learning-event oracles อ่านครบเพิ่มแล้ว; ข้อ lost-ACK เดิมครอบคลุม SRS ด้วย และพบ real recordAnswer + reopen test ที่ตรวจ SRS enqueue จริง; เพิ่ม achievement/assessment/assignment sync oracles และ research authority/permit/measurement domain/runtime composition แล้ว; เพิ่ม research persistence/proof, assignment context/consent และหน้าจอ research พร้อม oracles หลักแล้ว; authority/transport/lifecycle oracles ยังรออ่าน. คิว proof ที่ต้นคิวถูกปฏิเสธทั้งหมดเป็นประเด็นรอ cross-check; settings มีเส้นทางถอนความยินยอมในเครื่องแยกจากสถานะ remote permit. Active Recall authority ยังไม่มี production caller ที่พบ จึงไม่อ้างว่าเปิดใช้จริง. เพิ่ม goals/preferences persistence, progress/calendar/profile/policy และ share-card generation/store/preview พร้อม test oracles; ตรวจ owner upgrade แล้ว valid session configuration ถูก rebind เจ้าของ/hash จึงไม่เพิ่ม migrated-pack finding. เพิ่ม config/theme และ oracles, legacy models/services กับ caller search และ AI content pipeline ที่ไม่มี production caller; ไม่ใช้สูตรเก่าอ้างเป็น canonical evidence. เพิ่ม reading restart/launcher/session oracles ครบ และ account/deletion/vocabulary CRUD/import source กับ account oracles; ยังต้อง cross-check vocabulary route gates และ cancellation. เพิ่ม associative reading launcher/session และ pre/post assessment พร้อม synthetic assessment oracle; ยังไม่รันหรือเปิดใช้งานงานวิจัย. เพิ่มบัญชี/email reset/export/categories/ghost duel/AI tutor/settings และข้อค้นพบ password reset; source แอปยังไม่แก้. เพิ่ม History/Review Center/Today/Study Planning/goals/preferences/catalog/detail และ quest/rewards source พร้อม oracles ที่มี; การอ่าน real Drift tests ยังไม่ใช่ runtime pass. เพิ่ม scramble/dictation/speech/shadowing และ main navigation พร้อม test oracles; terminal recovery ที่สำเร็จไม่ได้ปิดข้อค้นพบ transient closing failure. เพิ่ม legacy matching/choose-mode/quiz/Pair launch และ CEFR/local-reading/route-voice พร้อม caller/test observations; sibling route gate ยังเป็นข้อสงสัยที่ต้อง reproduce จึงยังไม่เพิ่มจำนวน findings. เพิ่ม feedback/hint/handwriting/configuration/sentence UI, speech use cases และ sentence widget oracles; failed cleanup barrier เป็นข้อกำหนด deliberate fail-closed จาก test source ไม่เพิ่ม finding. เพิ่ม Today/Pair/parity/golden/offline/architecture และ performance integration/driver/runner/test-source observations ครบ; performance วัดเฉพาะ preparation/frame fixture และยังไม่ได้รัน B18 หรือรับรองอุปกรณ์. ข้อสงสัย MediaQuery wrapper ตรวจ local Flutter SDK แล้วไม่พบ defect: MaterialApp ไม่สร้าง MediaQuery เอง และ View รับ platform accessibility/textScaler จาก parent; result accessibility test อีกไฟล์ตรวจ effective2x โดยตรงด้วย.

เพิ่ม export reader/facade/archive/file store และ focused lifecycle/owner-upgrade/UI oracles ครบ: coherent owner snapshot, personal history หลัง withdrawal, rollback/backup และ typed cancellation; runtime sync integration ใช้ synthetic preflight/codec จึงไม่ปิด actual ACK mismatch. Legacy research helpers ไม่มี production caller ที่ค้นพบ; ไม่เปิดใช้หรือสร้างรายงานสถิติจากการอ่านนี้.

เพิ่ม voice pipeline/facade/factory/native/remote/offline pack/mirror source พร้อม test/voice ทั้ง30ไฟล์และ VoiceUseCases oracle; แยกความเสี่ยงใน dormant components จากเส้นทางที่แอปเรียกใช้จริง ยังไม่เพิ่ม runtime claim. Guest/background services และ legacy service callers ตรวจเพิ่มแล้ว

เพิ่ม backend voice/AI API source/config/test oracles และ structured lock provenance ครบ; AI Tutor source11ไฟล์และ test/features/ai_tutor7ไฟล์อ่านครบ รวม actual-loopback/credential recovery/usage journal oracles แต่ยังไม่รันใหม่. ประเด็น fallback เมื่อ local429, concurrent cache misses, malformed provider usage และการถอน consent ระหว่าง drain ยังต้อง cross-check; เพิ่ม candidate B18-REV08-01 การถอนสิทธิ์ที่เข้าคิวหลังรอ drain ทำให้คำขอแทรกได้ และ B18-REV08-02 ตัวเลขค่าใช้จ่ายขนาดใหญ่ทำให้ decoder ทิ้งคำตอบที่จ่ายแล้ว; ยังต้อง reproduce และแก้ใน G8.3.

เพิ่ม Gemini, media/camera/speech และ device model source พร้อม focused test oracles ครบ; mapping มี127รายการ/11หมวด ไม่มี normalized label ซ้ำ และ bootstrap ใช้ threshold เริ่มต้น .15. แบบประเมิน source6ไฟล์อ่านครบ; focused assessment tests อ่านครบแล้ว แต่ caller/owner/terminal recovery cross-check ยังไม่ปิด. ไม่มีการเปิดใช้งานวิจัยหรือผล device/runtime ใหม่.

เพิ่ม events, streak/timezone, offline content source/UI และ focused oracles ครบ; canonical streak ใช้ atomic receipt และ offline removal ตรวจ pin ภายใน lease. Restart บาง tests เป็นเพียง manager recreation บน memory DB; ไม่อ้าง process restart. รายละเอียดและข้อสังเกตอยู่ใน REV-12 journal; ไม่มี runtime result ใหม่.

เพิ่ม quests และ rewards source/UI/artwork พร้อม focused oracles ครบ: paired XP/coin ใช้ธุรกรรมเดียว และ shop ทดสอบ lost-ACK/readback retry โดยไม่ซื้อซ้ำ. Historical projection-rebuild-v2 test ไม่ได้สร้างยอดตามชื่อ จึงไม่นับเป็นหลักฐาน nonempty rebuild. ยังต้องตรวจ owner transition ข้ามระบบ; findings คง16ข้อ.

เพิ่ม reminders/review/time tracking/Today Hub source และ focused oracles พร้อม complete owner export/delete scenario ครบ; การลบมี actual file reopen และ full-row foreign/global preservation แต่ไม่ครอบคลุม native reminder cleanup เมื่อ lease เปลี่ยน. เวลาเรียนยึด monotonic โดยตั้งใจรองรับ wall-clock rollback. Today Hub ตรวจ owner ก่อน/หลังและมี full-row readonly oracle; ยังไม่มี runtime result หรือ zone acceptance ใหม่.

[Checkpoint integrity](../evidence/bundles/B18/checkpoint-integrity.json) ยืนยัน raw source เดิม3,610ไฟล์ตรง freeze; ไม่มี ignored source. G8.3 remediation และ runtime retest ยังไม่เริ่ม; ยังไม่ส่งต่อ B19. Application writer ยังเป็น taskนี้; ไม่มี background implementer.

คงข้อจำกัดจาก handoff: schema27/50, legacy originals/rollback backups, candidate disabled, canonical preview learning=true/cloud=false. Device/native/human/accessibility/live provider/cost/latency/installed flags/full release ยัง NOT RUN ตาม scope เดิม. Standard/default policy คงเดิม; effective service tier ยัง unverified.

เพิ่ม settings/dashboard/SRS/scanner/legacy screens และวิดเจ็ต พร้อมสัญญาฟีเจอร์4ไฟล์และ oracles ที่เกี่ยวข้อง; legacy progress ไม่มี production caller และ placeholders10ไฟล์ว่างจริง ไม่ใช่ export. บันทึกใหม่ B18-REV12-02: npm seed มี duplicated data splice ผิดไวยากรณ์ แต่ CLI oracle เดิมตรวจเพียง regex จึงไม่พิสูจน์ว่า parse ได้; ต้องยืนยันและแก้ใน G8.3 โดยไม่เรียก seed/cloud. Findings รวม17ข้อยังเปิดอยู่. เหลือ generated Drift/platform/config/backendLM/tooling และ test oracles; ไม่มี runtime result ใหม่.
