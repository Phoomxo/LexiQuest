# Research implementation checkpoint — 2026-09-05 12:23 Asia/Bangkok

> Historical snapshot only. The user subsequently authorized continued development
> and revised the recovery guardrails. See
> [current engineering status](2026-09-05-research-engineering-status.md) for the
> latest implementation and verification evidence. The pause below is not active.

## State and authorization

Research implementation is **incomplete and paused**, not production-ready.
The user authorized Research now, followed by a new user-visible Pair Matching
PM0–PM8 task only after Research is complete. No Pair task has been created.
No implementation commit has been made.

Worktree: `C:/Users/Phet/Documents/LexiQuest/.worktrees/research`.
Branch: `feature/adventure-research`, based on `6968ce97`.
The original `adventure-motivation-plan` worktree and its user-owned edits are untouched.

The main agent stopped after repeatedly attempting to read nonexistent source
paths during discovery, as required by the repository guardrail. These were
agent path-selection errors, not evidence that the repository is corrupt.
Resume by discovering paths with `rg --files` before opening any source file.
Both subagents stopped and reported no running processes. Do not retry the
unrelated prior temporary-worktree cleanup.

## Verified checkpoints (separate focused runs, not a full-suite result)

- R1 immutable, versioned bilingual paired instrument and scoring: **96 passed**;
  focused two-file analyzer clean (McClintock report under `.superpowers/sdd/`).
- R2 four tables at schema v24, cross-owner SQL guards and historical migrations:
  **58 passed** via `flutter test --no-pub test/database --reporter compact`.
- R3 initial permit import, active-owner/consent/assignment checks and measurement
  start/record/close/load: **9 passed** via
  `flutter test --no-pub test/features/research/drift_motivation_measurement_repository_test.dart --reporter expanded`.
- R4a safe research owner-upgrade conflict: **12 passed** (subagent report).

## Unfinished changes and failing tests

- R4a export/deletion: **5 passed, 3 failed**. Four manifest entries and deletion
  order exist; implement detailed allowlisted export projections, withdrawn/
  tombstoned fields and malformed response-code rejection. Production export
  reader/archive files have not yet been edited. No R4a analyzer run.
- P-256 verifier: **40 passed, 36 failed** in the expanded 76-case suite. Basic
  ECDSA verification exists, but strict canonical signature/key parsing,
  on-curve validation, exception-safe rejection and Unicode cases remain.
  It is **not production-ready**. PointyCastle remains version 4.0.0, now direct.
- The initial R3 tests use explicitly synthetic signature/receipt authorities;
  they do not establish end-to-end production cryptographic correctness.

## Next work, in order

1. Finish the bounded crypto and export RED suites above; inspect subagent edits.
2. Finish R3 durable opportunities and the four neutral behavior events, retry
   identity, transactional switch ordinals 1–10 and suppression, accepted canonical
   session links, first exposure/index completion and baseline/post timing tests.
   No such opportunity repository/event recorder exists yet.
3. Add immutable signed permit update/revocation import, capture gate and
   withdrawal lifecycle, including receipt/owner/expiry races and immediate
   checks at mutation boundaries. Initial `requireActiveRun` reloads row state
   but callers must avoid using stale timing/response snapshots across awaits.
4. Implement R4 sync collection payloads/rollout gates, outbox and upload-claim
   validation, Firestore rules and emulator tests. Current research repositories
   have **no research outbox integration yet**.
5. Implement R5 configurable real receipt/instrument/issuer authority and
   accessible enrollment/baseline/post/skip/withdraw UI. Today host/bootstrap
   remain unwired; production default must remain off and nonparticipant zero-row.
6. Stop implementation agents before repository-wide/local security checks.
   Run focused + full default/serial tests, analyzer, contract/generated plan
   checks, bounded Gitleaks/OSV and APK build/hash. Update source/evidence and review.
7. Only after Research engineering completion: `list_projects`, then create the
   requested new Pair task from the completed Research branch/source, and emit
   the created-thread directive. Do not invent ethics/device/UAT approval.

## Working-tree hygiene

- Some generated plugin registrars and the empty legacy
  `lib/learning/storage/learning_database.g.dart` show line-ending-only status
  changes after generation/pub-get. Inspect and exclude unrelated churn.
- `app_database.g.dart` was generated by the installed Drift builder and has a
  large formatting/generation diff; review proportionality before committing.
- `docs/superpowers/plans/2026-09-05-adventure-research-implementation.md` contains
  the agreed full six-group plan. Its unchecked boxes are not a completion claim.
- No security scanner, repository-wide analysis, or deployment ran concurrently
  with subagent implementation. No Codex Security workflow was used.
