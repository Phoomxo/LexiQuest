{
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "seed": "node lib/add_default_categories.js",
    "test:rules": "firebase emulators:exec --project demo-lexiquest-rules-test --only firestore \"node --test test/security/firestore-rules.test.cjs\"",
    "test:auth": "firebase emulators:exec --project demo-lexiquest-auth-test --only auth \"node --test test/security/firebase-auth-emulator.test.cjs\""
  },
  "dependencies": {
    "firebase-admin": "^14.2.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.16.0"
  }
}
